# 마커 사진 우선 대표 선정 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 같은 자리 스택 마커와 클러스터 마커가 사진 있는 글을 두고도 점으로 표시되는 문제를 없애고, 지도 score에서 본인글 보너스를 제거한다.

**Architecture:** 스택 그룹의 "랭킹 값"(크기 티어·zIndex·클러스터 선정)과 "표시 대표"(아이콘·캡션·카드 첫 장)를 분리한다. 두 선정 규칙을 `map_naver.dart` 밖의 순수 함수로 빼서 단위 테스트를 붙이고, 화면 코드는 그 함수를 호출만 한다. 백엔드는 score SQL의 본인글 보너스를 지우고 신선도 배점을 올린다.

**Tech Stack:** Dart / Flutter (flutter_test), Kotlin / Spring Boot (네이티브 SQL)

**설계 문서:** `docs/specs/2026-07-28-마커-사진-우선-대표-선정.md`

## Global Constraints

- 색상 하드코딩 금지. 색이 필요하면 `AppColors.of(context)` 시멘틱 토큰만 사용한다 (이 계획에는 색 변경이 없다).
- 마커 크기·줌·거리 상수는 `lib/screens/map/marker/marker_constants.dart` 에서만 정의한다. 새 상수가 필요하면 거기 추가한다.
- 각 태스크 종료 전 `flutter analyze` 통과 — 에러 0, 기존 info 50건 유지.
- 각 태스크 종료 전 `flutter test` 전건 통과.
- 커밋 메시지는 `[타입] 설명` 형식. 타입은 `feat` / `fix` / `refactor` / `test` / `docs` / `chore`.
- 커밋 메시지 끝에 `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` 를 붙인다.
- 백엔드 레포는 `/Users/marketdesignrs/myblog/unimal-server` 로 별도다. Task 5만 거기서 작업한다.

---

## File Structure

| 파일 | 책임 | 상태 |
| --- | --- | --- |
| `lib/screens/map/marker/marker_group.dart` | 좌표 그룹핑 키, 스택 그룹 대표 선정, 클러스터 대표 선정 — 화면 의존성 없는 순수 규칙 | 신규 |
| `test/screens/map/marker/marker_group_test.dart` | 위 규칙의 단위 테스트 | 신규 |
| `lib/screens/map/map_naver.dart` | 규칙 함수 호출로 전환. 인라인 그룹핑/선정 로직 제거 | 수정 |
| `test/screens/map/map_cluster_policy_test.dart` | 낡은 문자열 검증을 상수 불변식 검증으로 교체 | 수정 |
| `unimal-server/board/.../map/MapBoardRepositoryImpl.kt` | score SQL | 수정 |

`marker_group.dart` 는 `marker_score_tiers.dart` · `map_reload_policy.dart` 와 같은 자리에 놓이는 "화면에서 뺀 순수 규칙" 파일이다. 기존 패턴을 그대로 따른다.

---

## Task 0: 낡은 클러스터 정책 테스트 정상화

`test/screens/map/map_cluster_policy_test.dart` 는 **현재 실패한다.** 2026-07-13 클러스터 튜닝 이전 값(`NInclusiveRange(0, 15)`, `85.0`)을 소스 문자열로 검증하는데 코드가 바뀌었다. 빨간 스위트 위에서는 이후 태스크의 "테스트 통과" 확인이 무의미하므로 먼저 초록으로 만든다.

문자열 검증은 그대로 두면 또 낡는다. 상수의 **불변식**을 검증하도록 바꾼다.

**Files:**
- Modify: `test/screens/map/map_cluster_policy_test.dart` (전체 교체)

**Interfaces:**
- Consumes: `lib/screens/map/marker/marker_constants.dart` 의 `kClusterMergeDistances`, `kBubbleMinZoom`, `kTextCardEnterZoom`, `kTextCardExitZoom`, `kClusterTapZoom`
- Produces: 없음

- [ ] **Step 1: 현재 실패를 눈으로 확인**

Run: `flutter test test/screens/map/map_cluster_policy_test.dart`
Expected: FAIL — `does not contain 'enableZoomRange: const NInclusiveRange(0, 15)'`

- [ ] **Step 2: 테스트 파일 전체 교체**

`test/screens/map/map_cluster_policy_test.dart`:

```dart
import 'dart:io';

import 'package:flutter_test/flutter_test.dart';
import 'package:unimal/screens/map/marker/marker_constants.dart';

/// 클러스터링이 켜지는 줌 상한. `map_naver.dart` 의
/// `enableZoomRange: const NInclusiveRange(0, 16)` 와 짝이며,
/// 마지막 테스트가 둘이 어긋나지 않는지 지킨다.
const int kClusteringMaxZoom = 16;

void main() {
  test('병합 거리 표가 클러스터링 구간(0~16)을 빠짐없이, 겹치지 않게 덮는다', () {
    for (var zoom = 0; zoom <= kClusteringMaxZoom; zoom++) {
      final matched =
          kClusterMergeDistances.keys.where((r) => r.contains(zoom)).toList();
      expect(matched, hasLength(1), reason: '줌 $zoom 을 덮는 구간이 정확히 하나여야 한다');
    }
  });

  test('말풍선 minZoom 은 클러스터링 상한보다 크다', () {
    // 말풍선(충돌 숨김 켜짐)이 클러스터링 구간까지 살아 있으면 새 클러스터
    // 마커가 숨김 고착된다 (2026-07-19 마커 소실).
    expect(kBubbleMinZoom, greaterThan(kClusteringMaxZoom));
  });

  test('점↔말풍선 히스테리시스는 enter > exit > 말풍선 minZoom 순서다', () {
    expect(kTextCardEnterZoom, greaterThan(kTextCardExitZoom));
    expect(kTextCardExitZoom, greaterThan(kBubbleMinZoom));
  });

  test('클러스터 탭 줌은 클러스터링 상한을 넘어 클러스터가 확실히 풀린다', () {
    expect(kClusterTapZoom, greaterThan(kClusteringMaxZoom));
  });

  test('map_naver 의 enableZoomRange 상한이 kClusteringMaxZoom 과 일치한다', () {
    final source = File('lib/screens/map/map_naver.dart').readAsStringSync();
    expect(
      source,
      contains('enableZoomRange: const NInclusiveRange(0, $kClusteringMaxZoom)'),
    );
  });
}
```

- [ ] **Step 3: 통과 확인**

Run: `flutter test test/screens/map/map_cluster_policy_test.dart`
Expected: PASS — 5개 테스트 전부

`flutter_naver_map` 을 직접 import 하지 않는다. `marker_constants.dart` 가 이미 끌어오므로 `NInclusiveRange.contains` 를 그대로 쓸 수 있다. 만약 import 오류가 나면 `import 'package:flutter_naver_map/flutter_naver_map.dart';` 를 추가한다.

- [ ] **Step 4: 전체 스위트 초록 확인**

Run: `flutter test`
Expected: 전건 PASS (실패 0)

- [ ] **Step 5: 커밋**

```bash
git add test/screens/map/map_cluster_policy_test.dart
git commit -m "$(cat <<'EOF'
[test] 낡은 클러스터 정책 테스트를 상수 불변식 검증으로 교체

소스 문자열로 2026-07-13 이전 값(NInclusiveRange(0,15), 85.0)을 검증하다
실패 상태로 방치돼 있었다. 병합 거리 커버리지·말풍선 minZoom·히스테리시스
순서 같은 실제 불변식을 상수에서 직접 검증하도록 바꾼다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 1: 스택 그룹 대표 선정 규칙 추출

좌표 그룹핑과 대표 선정을 `map_naver.dart` 밖 순수 함수로 뺀다. 랭킹 score(그룹 최댓값)와 표시 대표(사진 우선)를 함께 들고 나온다.

**Files:**
- Create: `lib/screens/map/marker/marker_group.dart`
- Test: `test/screens/map/marker/marker_group_test.dart`

**Interfaces:**
- Consumes: `kStackGroupPrecision` (`marker_constants.dart`), `MapPost` (`lib/service/map/models/map_post.dart`)
- Produces:
  - `String stackGroupKey(double lat, double lng)`
  - `class MarkerGroup { List<MapPost> posts; double rankScore; MapPost get representative; bool get hasPhoto; }`
  - `List<MarkerGroup> buildMarkerGroups(Iterable<MapPost> posts)`

- [ ] **Step 1: 실패하는 테스트 작성**

`test/screens/map/marker/marker_group_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:unimal/screens/map/marker/marker_group.dart';
import 'package:unimal/service/board/model/file_info.dart';
import 'package:unimal/service/map/models/map_post.dart';

MapPost _post({
  required String id,
  required double score,
  int files = 0,
  double lat = 37.5,
  double lng = 127.0,
}) {
  return MapPost(
    id: id,
    nickname: 'n',
    title: 't',
    content: 'c',
    streetName: 's',
    latitude: lat,
    longitude: lng,
    createdAt: '2026-07-28T00:00:00',
    fileInfoList: List.generate(
      files,
      (i) => FileInfo(fileId: '$id-$i', fileUrl: 'https://cdn/$id-$i.png'),
    ),
    likeCount: 0,
    replyCount: 0,
    score: score,
    isOwner: false,
    isLike: false,
  );
}

void main() {
  group('buildMarkerGroups', () {
    test('사진 글이 최고 score 가 아니어도 표시 대표가 된다', () {
      final groups = buildMarkerGroups([
        _post(id: 'text', score: 10004.1),
        _post(id: 'photo', score: 10004.0, files: 1),
      ]);

      expect(groups, hasLength(1));
      expect(groups.single.representative.id, 'photo');
      expect(groups.single.hasPhoto, isTrue);
    });

    test('랭킹 score 는 대표가 아니라 그룹 최댓값을 유지한다', () {
      final groups = buildMarkerGroups([
        _post(id: 'text', score: 10004.1),
        _post(id: 'photo', score: 3.1, files: 1),
      ]);

      expect(groups.single.representative.id, 'photo');
      expect(groups.single.rankScore, 10004.1);
    });

    test('대표를 앞으로 옮겨도 나머지는 score 내림차순을 유지한다', () {
      final groups = buildMarkerGroups([
        _post(id: 'text-hi', score: 100),
        _post(id: 'photo', score: 20, files: 2),
        _post(id: 'text-mid', score: 50),
        _post(id: 'text-lo', score: 10),
      ]);

      expect(
        groups.single.posts.map((p) => p.id).toList(),
        ['photo', 'text-hi', 'text-mid', 'text-lo'],
      );
    });

    test('사진 글이 없으면 최고 score 글이 대표이고 hasPhoto 는 false', () {
      final groups = buildMarkerGroups([
        _post(id: 'lo', score: 1),
        _post(id: 'hi', score: 9),
      ]);

      expect(groups.single.representative.id, 'hi');
      expect(groups.single.rankScore, 9);
      expect(groups.single.hasPhoto, isFalse);
    });

    test('사진 글이 여럿이면 그중 최고 score 가 대표', () {
      final groups = buildMarkerGroups([
        _post(id: 'photo-lo', score: 5, files: 1),
        _post(id: 'text', score: 99),
        _post(id: 'photo-hi', score: 7, files: 3),
      ]);

      expect(groups.single.representative.id, 'photo-hi');
      expect(groups.single.rankScore, 99);
    });

    test('11m 타일이 다르면 별개 그룹이다', () {
      // kStackGroupPrecision = 4 → 소수 넷째 자리가 다르면 다른 타일.
      final groups = buildMarkerGroups([
        _post(id: 'a', score: 1, lat: 37.5000),
        _post(id: 'b', score: 2, lat: 37.5001),
      ]);

      expect(groups, hasLength(2));
    });

    test('빈 입력은 빈 결과', () {
      expect(buildMarkerGroups(const <MapPost>[]), isEmpty);
    });
  });

  group('stackGroupKey', () {
    test('같은 11m 타일이면 같은 키', () {
      expect(stackGroupKey(37.50001, 127.00001),
          stackGroupKey(37.50004, 127.00004));
    });

    test('타일이 다르면 다른 키', () {
      expect(stackGroupKey(37.5000, 127.0),
          isNot(stackGroupKey(37.5001, 127.0)));
    });
  });
}
```

- [ ] **Step 2: 실패 확인**

Run: `flutter test test/screens/map/marker/marker_group_test.dart`
Expected: FAIL — `Target of URI doesn't exist: 'package:unimal/screens/map/marker/marker_group.dart'`

- [ ] **Step 3: 구현**

`lib/screens/map/marker/marker_group.dart`:

```dart
import 'package:unimal/screens/map/marker/marker_constants.dart';
import 'package:unimal/service/map/models/map_post.dart';

/// 같은 자리 스택 그룹핑 좌표 키 ([kStackGroupPrecision], 4자리 ≈ 11m 타일).
/// 그룹핑과 펼침 숨김 가드가 반드시 같은 키를 쓰도록 한 곳에서 관리한다.
String stackGroupKey(double lat, double lng) =>
    '${lat.toStringAsFixed(kStackGroupPrecision)},'
    '${lng.toStringAsFixed(kStackGroupPrecision)}';

/// 같은 자리(11m 타일) 글 묶음.
///
/// **랭킹과 표시를 분리해서 들고 있다** (설계: docs/specs/2026-07-28).
/// - [rankScore] — 이 자리가 얼마나 중요한가. 크기 티어·zIndex·클러스터 대표 선정.
/// - [representative] — 무엇을 보여줄까. 아이콘·캡션·카드 첫 장·탭 대상.
///
/// 분리하지 않으면 참여도 높은 텍스트 글을 품은 스택이 대표가 사진 글로 바뀌면서
/// 하위 티어(42dp)로 내려앉는다.
class MarkerGroup {
  /// 표시 순서. `[0]` 이 표시 대표(사진 우선), 나머지는 score 내림차순.
  final List<MapPost> posts;

  /// 그룹 내 최고 score. 대표 글의 score 와 다를 수 있다.
  final double rankScore;

  const MarkerGroup({required this.posts, required this.rankScore});

  /// 표시 대표 — 사진 있는 글 중 최고 score. 사진이 없으면 최고 score 글.
  MapPost get representative => posts.first;

  /// 그룹에 사진이 하나라도 있는가. 사진이 있으면 반드시 대표가 사진 글이므로
  /// 대표만 보면 판정된다.
  bool get hasPhoto => posts.first.fileInfoList.isNotEmpty;
}

/// 글 목록을 11m 타일로 묶고, 그룹마다 랭킹 score 와 표시 대표를 정한다.
///
/// 1. score 내림차순 정렬 → 첫 원소가 [MarkerGroup.rankScore]
/// 2. 사진 있는 글 중 최고 score(=정렬 후 첫 사진 글)를 맨 앞으로 이동
///
/// 사진 글이 없거나 이미 맨 앞이면 순서를 건드리지 않는다.
List<MarkerGroup> buildMarkerGroups(Iterable<MapPost> posts) {
  final Map<String, List<MapPost>> grouped = {};
  for (final post in posts) {
    grouped
        .putIfAbsent(stackGroupKey(post.latitude, post.longitude), () => [])
        .add(post);
  }

  return grouped.values.map((list) {
    list.sort((a, b) => b.score.compareTo(a.score));
    final rankScore = list.first.score; // 정렬 직후 = 그룹 최댓값
    final photoIdx = list.indexWhere((p) => p.fileInfoList.isNotEmpty);
    if (photoIdx > 0) list.insert(0, list.removeAt(photoIdx));
    return MarkerGroup(posts: list, rankScore: rankScore);
  }).toList();
}
```

- [ ] **Step 4: 통과 확인**

Run: `flutter test test/screens/map/marker/marker_group_test.dart`
Expected: PASS — 9개 테스트 전부

- [ ] **Step 5: 정적 분석 + 전체 스위트**

Run: `flutter analyze && flutter test`
Expected: analyze 에러 0 / 테스트 전건 PASS

- [ ] **Step 6: 커밋**

```bash
git add lib/screens/map/marker/marker_group.dart test/screens/map/marker/marker_group_test.dart
git commit -m "$(cat <<'EOF'
[feat] 스택 그룹 대표 선정 규칙 추출 — 랭킹과 표시 분리

buildMarkerGroups 가 11m 타일 그룹핑과 함께 두 값을 정한다.
- rankScore: 그룹 최댓값 (크기 티어·zIndex·클러스터 선정용)
- representative: 사진 있는 글 중 최고 score (아이콘·캡션·카드용)

사진 글을 올렸는데 같은 자리 텍스트 글의 score 가 높아 점 마커로
그려지던 문제의 근거 규칙. 화면 적용은 후속 커밋.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: 클러스터 대표 선정 규칙 추가

클러스터 빌더는 자식의 `tags` 만 볼 수 있다. 선정에 필요한 값(`hasPhoto`, `score`)만 추린 입력으로 순수 함수를 만든다.

**Files:**
- Modify: `lib/screens/map/marker/marker_group.dart` (파일 끝에 추가)
- Modify: `test/screens/map/marker/marker_group_test.dart` (`group` 블록 추가)

**Interfaces:**
- Consumes: 없음 (외부 타입 의존 없음)
- Produces:
  - `class ClusterChildRank { bool hasPhoto; double score; }`
  - `int pickClusterRepIndex(List<ClusterChildRank> children)` — 선정된 자식의 인덱스. 빈 리스트면 `-1`.

- [ ] **Step 1: 실패하는 테스트 작성**

`test/screens/map/marker/marker_group_test.dart` 의 `main()` 안, 기존 `group` 블록들 뒤에 추가:

```dart
  group('pickClusterRepIndex', () {
    test('사진 자식이 최고 score 가 아니어도 선정된다', () {
      final idx = pickClusterRepIndex(const [
        ClusterChildRank(hasPhoto: false, score: 10001),
        ClusterChildRank(hasPhoto: true, score: 20.1),
        ClusterChildRank(hasPhoto: true, score: 8.1),
      ]);
      expect(idx, 1); // 사진 자식 중 최고 score
    });

    test('사진 자식이 없으면 최고 score 자식', () {
      final idx = pickClusterRepIndex(const [
        ClusterChildRank(hasPhoto: false, score: 3),
        ClusterChildRank(hasPhoto: false, score: 9),
        ClusterChildRank(hasPhoto: false, score: 5),
      ]);
      expect(idx, 1);
    });

    test('동점이면 먼저 오는 자식', () {
      final idx = pickClusterRepIndex(const [
        ClusterChildRank(hasPhoto: true, score: 7),
        ClusterChildRank(hasPhoto: true, score: 7),
      ]);
      expect(idx, 0);
    });

    test('음수 score 도 정상 비교된다', () {
      final idx = pickClusterRepIndex(const [
        ClusterChildRank(hasPhoto: false, score: -5),
        ClusterChildRank(hasPhoto: false, score: -1),
      ]);
      expect(idx, 1);
    });

    test('빈 리스트면 -1', () {
      expect(pickClusterRepIndex(const []), -1);
    });
  });
```

- [ ] **Step 2: 실패 확인**

Run: `flutter test test/screens/map/marker/marker_group_test.dart`
Expected: FAIL — `Undefined name 'pickClusterRepIndex'`

- [ ] **Step 3: 구현**

`lib/screens/map/marker/marker_group.dart` 파일 끝에 추가:

```dart
/// [pickClusterRepIndex] 입력 — 클러스터 자식 마커에서 선정에 필요한 값만 추린 것.
/// 플러그인 타입(`NClusterableMarkerInfo`)에 의존하지 않아 단위 테스트가 가능하다.
class ClusterChildRank {
  /// 이 자식 마커가 사진 썸네일을 갖고 있는가 (`tags['hasPhoto'] == '1'`).
  final bool hasPhoto;

  /// 이 자식 마커의 랭킹 score (`tags['score']`).
  final double score;

  const ClusterChildRank({required this.hasPhoto, required this.score});
}

/// 클러스터 표시 대표를 고른다 — **사진 자식 우선, 없으면 최고 score 자식.**
///
/// 스택 마커와 같은 규칙([buildMarkerGroups])을 클러스터에도 적용해, 사진 글을
/// 품은 클러스터가 점으로 그려지지 않게 한다. 동점이면 먼저 오는 자식을 쓴다.
///
/// 반환값은 [children] 의 인덱스. 빈 리스트면 `-1`.
int pickClusterRepIndex(List<ClusterChildRank> children) {
  var bestIdx = -1;
  var photoIdx = -1;
  var bestScore = double.negativeInfinity;
  var photoScore = double.negativeInfinity;

  for (var i = 0; i < children.length; i++) {
    final child = children[i];
    if (child.score > bestScore) {
      bestScore = child.score;
      bestIdx = i;
    }
    if (child.hasPhoto && child.score > photoScore) {
      photoScore = child.score;
      photoIdx = i;
    }
  }
  return photoIdx >= 0 ? photoIdx : bestIdx;
}
```

- [ ] **Step 4: 통과 확인**

Run: `flutter test test/screens/map/marker/marker_group_test.dart`
Expected: PASS — 14개 테스트 전부

- [ ] **Step 5: 정적 분석 + 전체 스위트**

Run: `flutter analyze && flutter test`
Expected: analyze 에러 0 / 테스트 전건 PASS

- [ ] **Step 6: 커밋**

```bash
git add lib/screens/map/marker/marker_group.dart test/screens/map/marker/marker_group_test.dart
git commit -m "$(cat <<'EOF'
[feat] 클러스터 대표 선정 규칙 추가 — 사진 자식 우선

pickClusterRepIndex 가 사진 있는 자식 중 최고 score 를 고르고,
없으면 최고 score 자식으로 폴백한다. 스택 마커와 같은 규칙.
화면 적용은 후속 커밋.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: 메인 지도 스택 경로를 새 규칙으로 전환

**Files:**
- Modify: `lib/screens/map/map_naver.dart`
  - `_loadMapMarkersInternal` 그룹핑부 (현재 `772-786`)
  - 마커 빌드 루프의 score 사용처 (`865`, `866`, `978`, `997`)
  - 디버그 로그 (`806-811`)
  - `_stackGroupKey` 정의 제거 (`1390`) 및 호출부 (`1022`, `1105`, `2077`)

**Interfaces:**
- Consumes: Task 1의 `buildMarkerGroups`, `MarkerGroup`, `stackGroupKey`
- Produces: 없음 (내부 전환)

- [ ] **Step 1: import 추가**

`map_naver.dart` 상단 import 블록에 추가 (`marker_constants.dart` import 옆):

```dart
import 'package:unimal/screens/map/marker/marker_group.dart';
```

- [ ] **Step 2: 그룹핑부 교체**

`772-786` 의 인라인 그룹핑/정렬/티어 생성을 교체한다.

교체 전:

```dart
    final Map<String, List<MapPost>> grouped = {};
    for (final post in posts) {
      final key = _stackGroupKey(post.latitude, post.longitude);
      grouped.putIfAbsent(key, () => []).add(post);
    }
    // 그룹 내 score 내림차순 — 첫 글이 대표(마커 아이콘·캡션·zIndex 기준)
    for (final list in grouped.values) {
      list.sort((a, b) => b.score.compareTo(a.score));
    }

    final List<List<MapPost>> markerGroups = grouped.values.toList();

    // score 크기 위계 — 화면에 로드된 마커(그룹 대표 score)의 상대 백분위.
    final tiers =
        MarkerScoreTiers.fromScores(markerGroups.map((g) => g.first.score));
```

교체 후:

```dart
    // 11m 타일 그룹핑 + 대표 선정은 marker_group.dart 의 순수 규칙에 위임한다.
    // 랭킹 score(그룹 최댓값)와 표시 대표(사진 우선)가 분리돼 나온다
    // (설계: docs/specs/2026-07-28-마커-사진-우선-대표-선정.md).
    final groups = buildMarkerGroups(posts);
    final List<List<MapPost>> markerGroups = [for (final g in groups) g.posts];
    // 표시 대표 id → 그룹 랭킹 score. 아래 빌드 루프가 크기·zIndex·태그에 쓴다.
    final Map<String, double> rankScoreById = {
      for (final g in groups) g.representative.id: g.rankScore,
    };

    // score 크기 위계 — 화면에 로드된 그룹들의 **랭킹 score** 상대 백분위.
    // 표시 대표 score 를 쓰면 사진 우선 이동 때문에 위계가 뒤틀린다.
    final tiers = MarkerScoreTiers.fromScores(groups.map((g) => g.rankScore));
```

- [ ] **Step 3: 디버그 로그에 랭킹 score 추가**

`806-811` 의 `stack group:` 로그를 교체한다. 대표 score 와 랭킹 score 가 이제 다를 수 있으므로 둘 다 남긴다.

교체 전 주석 3줄 + 블록 전체를 아래로 바꾼다:

```dart
    // 스택 그룹 구성 추적 — 표시 대표(첫 글, 사진 우선)와 랭킹 score(그룹
    // 최댓값)가 다를 수 있으므로 둘 다 남긴다. "사진 글인데 점 마커" 추적용.
    if (kDebugMode) {
      for (final g in groups.where((g) => g.posts.length > 1)) {
        debugPrint('[map] stack group rank=${g.rankScore} '
            'rep=${g.representative.id} → ${g.posts.map((p) => '${p.id}'
            '(score=${p.score} files=${p.fileInfoList.length})').join(' | ')}');
      }
    }
```

- [ ] **Step 4: 빌드 루프의 score 사용처 4곳 교체**

루프 안 `final topPost = group.first;` 바로 아래에 랭킹 score 를 꺼낸다:

```dart
      // 랭킹 score — 그룹 최댓값. 표시 대표(topPost)의 score 와 다를 수 있다.
      final double rankScore = rankScoreById[topPost.id] ?? topPost.score;
```

그리고 네 곳을 바꾼다:

| 위치 | 전 | 후 |
| --- | --- | --- |
| `865` | `tiers.sizeFor(topPost.score)` | `tiers.sizeFor(rankScore)` |
| `866` | `tiers.isHot(topPost.score)` | `tiers.isHot(rankScore)` |
| `978` | `'score': topPost.score.toString(),` | `'score': rankScore.toString(),` |
| `997` | `final baseZIndex = 200000 + topPost.score.toInt();` | `final baseZIndex = 200000 + rankScore.toInt();` |

- [ ] **Step 5: `_stackGroupKey` 를 공용 함수로 교체**

`1390` 의 private 정의를 삭제한다:

```dart
  /// 같은 자리 스택 그룹핑 좌표 키 (kStackGroupPrecision, ≈11m 타일).
  /// 그룹핑과 펼침 숨김 가드가 반드시 같은 키를 쓰도록 한 곳에서 관리.
  String _stackGroupKey(double lat, double lng) =>
      '${lat.toStringAsFixed(kStackGroupPrecision)},'
      '${lng.toStringAsFixed(kStackGroupPrecision)}';
```

남은 호출부 3곳(`1022`, `1105`, `2077`)의 `_stackGroupKey(` 를 `stackGroupKey(` 로 바꾼다.

```bash
# 정의 삭제 후 호출부 일괄 치환 결과를 확인한다
grep -n "_stackGroupKey" lib/screens/map/map_naver.dart
```
Expected: 결과 없음

- [ ] **Step 6: 정적 분석**

Run: `flutter analyze`
Expected: 에러 0. `kStackGroupPrecision` 이 map_naver 에서 더는 안 쓰이면 unused import 경고가 날 수 있는데, `marker_constants.dart` 의 다른 상수를 계속 쓰므로 import 는 유지된다.

- [ ] **Step 7: 전체 스위트**

Run: `flutter test`
Expected: 전건 PASS

- [ ] **Step 8: 실기기/시뮬레이터 확인**

Run: `flutter run`

확인 항목:
1. 같은 자리에 사진 글 + score 더 높은 텍스트 글이 있을 때 마커가 **썸네일**
2. `[map] stack group rank=... rep=...` 로그에서 `rep` 이 사진 글 id
3. 참여도 높은 텍스트 글을 품은 스택의 크기가 하위 티어(42dp)로 떨어지지 않음
4. 마커 탭 → 카드 첫 장이 사진 글

- [ ] **Step 9: 커밋**

```bash
git add lib/screens/map/map_naver.dart
git commit -m "$(cat <<'EOF'
[fix] 스택 마커가 사진 글을 두고 점으로 그려지던 문제

그룹핑/대표 선정을 buildMarkerGroups 에 위임하고, 크기 티어·zIndex·
score 태그는 그룹 최댓값(rankScore)을, 아이콘·캡션·카드 첫 장은 사진
우선 대표를 쓰도록 분리했다.

_stackGroupKey 는 marker_group.dart 의 stackGroupKey 로 통합.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: 클러스터 빌더를 새 규칙으로 전환

**Files:**
- Modify: `lib/screens/map/map_naver.dart`
  - 마커 생성부 `tags` (현재 `973-982` 부근)
  - `_buildClusterMarker` 선정 루프 (현재 `1652-1690` 부근)

**Interfaces:**
- Consumes: Task 2의 `ClusterChildRank`, `pickClusterRepIndex`
- Produces: 없음 (내부 전환)

- [ ] **Step 1: `hasPhoto` 태그 추가**

`NClusterableMarker(... tags: {...})` 의 태그 맵에 한 줄 추가한다. `count` 다음 줄:

```dart
          // 클러스터 대표 선정용 — 이 그룹에 사진이 하나라도 있는가.
          // 표시 대표는 사진 우선이므로 대표만 보면 판정된다.
          'hasPhoto': topPost.fileInfoList.isEmpty ? '0' : '1',
```

- [ ] **Step 2: 선정 루프 교체**

`_buildClusterMarker` 의 `try {` 직후, `if (topId != null) {` 앞까지의 선정 블록을 교체한다.

교체 전:

```dart
    // children 중 score 최대 마커 식별 + 스택 글 수 합산
    String? topId;
    String? topTitle;
    NLatLng? topPosition;
    bool topCanCard = false;
    int topOwnCount = 1;
    double topScore = -1;
    // 클러스터가 품은 총 글 수 — 스택 마커(count 2+)를 포함하므로
    // 마커 수(info.size)가 아니라 count 태그 합계를 뱃지에 표시.
    int totalCount = 0;
    for (final child in info.children) {
      final childCount = int.tryParse(child.tags['count'] ?? '1') ?? 1;
      totalCount += childCount;
      final s = double.tryParse(child.tags['score'] ?? '0') ?? 0;
      if (s > topScore) {
        topScore = s;
        topId = child.id;
        topTitle = child.tags['title'];
        topPosition = child.position;
        topCanCard = child.tags['canCard'] == '1';
        topOwnCount = childCount;
      }
    }
```

교체 후:

```dart
    // 표시 대표는 **사진 자식 우선**, 없으면 최고 score 자식
    // (스택 마커와 같은 규칙 — docs/specs/2026-07-28).
    // 클러스터가 품은 총 글 수는 마커 수(info.size)가 아니라 count 태그 합계다.
    final children = info.children;
    final ranks = <ClusterChildRank>[];
    int totalCount = 0;
    for (final child in children) {
      totalCount += int.tryParse(child.tags['count'] ?? '1') ?? 1;
      ranks.add(ClusterChildRank(
        hasPhoto: child.tags['hasPhoto'] == '1',
        score: double.tryParse(child.tags['score'] ?? '0') ?? 0,
      ));
    }

    final repIdx = pickClusterRepIndex(ranks);
    String? topId;
    String? topTitle;
    NLatLng? topPosition;
    bool topCanCard = false;
    int topOwnCount = 1;
    double topScore = -1;
    if (repIdx >= 0) {
      final rep = children[repIdx];
      topId = rep.id;
      topTitle = rep.tags['title'];
      topPosition = rep.position;
      topCanCard = rep.tags['canCard'] == '1';
      topOwnCount = int.tryParse(rep.tags['count'] ?? '1') ?? 1;
      topScore = ranks[repIdx].score;
    }
```

이후 코드(`if (topId != null) { ... }`, 캡션, 탭 리스너)는 그대로 둔다 — 전부 `topId`/`topTitle`/`topPosition`/`topCanCard`/`topOwnCount` 를 쓰므로 자동으로 새 대표를 따른다.

- [ ] **Step 3: 정적 분석**

Run: `flutter analyze`
Expected: 에러 0

- [ ] **Step 4: 전체 스위트**

Run: `flutter test`
Expected: 전건 PASS

- [ ] **Step 5: 실기기/시뮬레이터 확인**

Run: `flutter run`

확인 항목:
1. 사진 마커 여러 개 + 텍스트 단독 마커(최고 score)가 있는 지역에서 줌아웃 → 클러스터가 **썸네일 + N 뱃지**
2. `[map] clusterBuilder top=` 로그의 선정 id 가 사진 자식
3. `+N` 뱃지 숫자가 전체 글 수 그대로 (대표가 바뀌어도 개수 불변)
4. 클러스터 탭 → 사진 자식 위치로 z18 이동
5. `[map] clusterBuilder NO ICON` 로그가 뜨지 않음

- [ ] **Step 6: 커밋**

```bash
git add lib/screens/map/map_naver.dart
git commit -m "$(cat <<'EOF'
[fix] 클러스터가 사진 글을 품고도 점으로 그려지던 문제

자식 마커에 hasPhoto 태그를 싣고, pickClusterRepIndex 로 사진 자식을
우선 선정한다. 아이콘·캡션·탭 위치·스택 판정이 모두 선정된 자식을
따르며, +N 뱃지 숫자는 전체 자식 합계 그대로다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: 백엔드 score — 본인글 보너스 제거 + 신선도 상향

**별도 레포에서 작업한다:** `/Users/marketdesignrs/myblog/unimal-server`

board 모듈에는 리포지토리 테스트가 없다 (`BoardApplicationTests.kt` 컨텍스트 로드 테스트뿐). 자동 테스트 대신 컴파일 + 실제 응답 확인으로 검증한다.

**Files:**
- Modify: `board/src/main/kotlin/com/unimal/board/domain/board/map/MapBoardRepositoryImpl.kt` (score 계산 SQL)

**Interfaces:**
- Consumes: 없음
- Produces: `MapPostInfo.score` 값 범위 변화. 앱은 score 를 숫자로만 쓰므로 프론트 변경 불필요.

- [ ] **Step 1: SQL 수정**

`findLocationPosts` 의 score 계산 블록을 교체한다.

교체 전:

```kotlin
                (
                  CASE WHEN b.email = :userEmail THEN 10000.0 ELSE 0.0 END
                  + CASE
                      WHEN b.created_at >= NOW() - INTERVAL '30 minutes' THEN 1.0
                      WHEN b.created_at >= NOW() - INTERVAL '2 hours'    THEN 0.6
                      WHEN b.created_at >= NOW() - INTERVAL '6 hours'    THEN 0.3
                      ELSE 0.1
                    END
```

교체 후:

```kotlin
                (
                  -- 본인글 보너스(+10000) 제거 (2026-07-28).
                  -- 내 글이 어디서나 최상위가 되어 마커 크기·캡션·클러스터 대표를
                  -- 독점했다. "내 글 보기" 필터가 따로 있으므로 기본 탐색에서는
                  -- 모두에게 공평한 노출 기회를 준다.
                  --
                  -- 대신 신선도 배점을 올린다 — 보너스만 빼면 새 글 score 가 4점대로
                  -- 떨어져 ORDER BY score DESC LIMIT 에서 밀려 방금 올린 글이 지도에
                  -- 안 보인다. 30분간 상위권에 올렸다가 자연 하락시킨다.
                  CASE
                      WHEN b.created_at >= NOW() - INTERVAL '30 minutes' THEN 20.0
                      WHEN b.created_at >= NOW() - INTERVAL '2 hours'    THEN 8.0
                      WHEN b.created_at >= NOW() - INTERVAL '6 hours'    THEN 3.0
                      ELSE 0.1
                    END
```

`is_owner` 컬럼과 `:userEmail` 파라미터는 **건드리지 않는다** — 마커 링 primary 색과 `is_like` 계산에 계속 쓰인다.

- [ ] **Step 2: 컴파일 확인**

```bash
cd /Users/marketdesignrs/myblog/unimal-server
./gradlew :board:compileKotlin
```
Expected: BUILD SUCCESSFUL

- [ ] **Step 3: `:userEmail` 파라미터가 여전히 SQL 안에 있는지 확인**

네이티브 쿼리는 바인딩된 파라미터가 SQL에 없으면 런타임 예외가 난다. `is_owner` / `is_like` 에 남아 있어야 한다.

```bash
grep -c ':userEmail' board/src/main/kotlin/com/unimal/board/domain/board/map/MapBoardRepositoryImpl.kt
```
Expected: 3 이상 (is_owner 1, is_like 1, setParameter 1)

- [ ] **Step 4: 로컬 기동 + 응답 확인**

board 서비스를 띄우고 지도 조회 API 를 호출해 score 를 눈으로 확인한다.

Expected:
- 방금 올린 사진 글 → `score ≈ 23.0` (20 신선도 + 3 사진)
- 방금 올린 텍스트 글 → `score ≈ 20.0`
- 10000 이상 값이 **하나도 없음**
- `is_owner` 가 내 글에 대해 여전히 `true`

- [ ] **Step 5: 커밋**

```bash
cd /Users/marketdesignrs/myblog/unimal-server
git add board/src/main/kotlin/com/unimal/board/domain/board/map/MapBoardRepositoryImpl.kt
git commit -m "$(cat <<'EOF'
[fix] 지도 score 본인글 보너스 제거 + 신선도 배점 상향

본인글 +10000 이 내 글을 어디서나 최상위로 만들어 마커 크기·캡션·
클러스터 대표를 독점했다. "내 글 보기" 필터가 별도로 있으므로 기본
탐색에서는 제거한다.

보너스만 빼면 새 글이 limit 컷에서 밀리므로 신선도 배점을 함께 올린다.
  30분 1.0 → 20.0 / 2시간 0.6 → 8.0 / 6시간 0.3 → 3.0 (그 외 0.1 유지)

is_owner 컬럼은 마커 링 색 판정에 계속 쓰이므로 유지.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 6: 앱에서 최종 확인**

백엔드 배포 후 앱에서 확인한다.

1. 방금 올린 글의 마커가 지도에 보이는가 (limit 컷 통과)
2. 내 글 마커 링이 여전히 **primary(파랑)** 인가 (`is_owner` 유지 확인)
3. 마커 크기 위계가 참여도 기준으로 분포하는가 — 내 글만 66dp 인 현상이 사라졌는가
4. 클러스터 대표 캡션이 내 새 글로 고정되지 않는가

---

## 검증 요약

| 항목 | 방법 |
| --- | --- |
| 스택 대표 선정 규칙 | `flutter test test/screens/map/marker/marker_group_test.dart` |
| 클러스터 대표 선정 규칙 | 같음 |
| 클러스터 정책 불변식 | `flutter test test/screens/map/map_cluster_policy_test.dart` |
| 화면 통합 | `flutter run` + Task 3 Step 8 / Task 4 Step 5 체크리스트 |
| 백엔드 score | Task 5 Step 4 / Step 6 |
| 정적 분석 | `flutter analyze` — 에러 0, info 50건 유지 |

## 이 계획의 범위 밖

설계 문서 "범위 밖" 절과 동일하다. 마이페이지 내지도, 업로드 후 카메라 이동, 줌 19 조회 반경, 개별 `deleteOverlay` N회 성능 — 전부 별건이다.
