# 메인 지도 바텀카드 피드 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 메인 지도 하단에 상시 바텀시트를 두고 `GET /board/map/feed` 의 섹션 피드를 보여준다. 카드 탭 시 확장카드가 열리고, 주소를 탭하면 그 좌표로 카메라가 이동한다.

**Architecture:** 시트가 자기 데이터를 소유한다. `map_naver` 는 `ValueNotifier<MapFeedQuery?>` 만 갱신하고 시트가 그것을 듣는다 (`map_naver.dart` 가 이미 2,900줄이라 상태를 더 얹지 않는다). 서버 미구현 기간에는 `.env.local` 플래그로 목 JSON 을 실제 파서에 태워 UI 를 확인한다.

**Tech Stack:** Flutter 3.38.7 / Dart, GetX(기존), `DraggableScrollableSheet`, `flutter_dotenv`, `cached_network_image`

설계 문서: `docs/specs/2026-07-29-지도-바텀카드-피드.md`
서버 계약: `unimal-server` `docs/specs/2026-07-29-지도-바텀카드-피드-api.md` (**서버 미구현**)

## Global Constraints

- **색상 하드코딩 금지.** `AppColors.of(context)` 시멘틱 토큰만 사용. 사용 가능 토큰: `primary primaryStrong primarySoft primaryWash background surface surfaceVariant surfaceMuted textPrimary textSecondary textTertiary textMuted border borderStrong divider danger accent accentCoral kakao shadow`. `Colors.transparent` 만 예외.
- 폰트는 `fontFamily: 'Pretendard'`.
- **중간 커밋 금지.** 커밋은 Task 7 에서 **한 번만** 한다. 태스크 사이에는 `flutter analyze` / `flutter test` 로만 검증한다 (`CLAUDE.md` 규칙 — 스킬·계획서보다 우선).
- **API 응답 JSON 키는 snake_case.** Dart 프로퍼티는 camelCase 로 두고 **`fromJson` 의 키 문자열만** snake_case 로 읽는다 (`CLAUDE.md` §네이밍 규칙 — 이 규칙이 이 계획서와 `unimal-server` 의 피드 API 스펙 문서보다 우선한다).

  이 피드 모델에서 변환이 필요한 키:

  | Dart 프로퍼티 | JSON 키 |
  | --- | --- |
  | `boardId` | `board_id` |
  | `thumbnailUrl` | `thumbnail_url` |
  | `streetName` | `street_name` |
  | `profileImage` | `profile_image` |
  | `likeCount` | `like_count` |
  | `replyCount` | `reply_count` |
  | `createdAt` | `created_at` |
  | `hasMore` | `has_more` |

  한 단어 키는 그대로다: `type` `title` `content` `dong` `latitude` `longitude` `nickname` `items` `sections`.

  서버는 클래스 레벨 `@JsonNaming(SnakeCaseStrategy)` 로 이 표기를 만든다(필드별 `@JsonProperty` 나열 금지 — `unimal-server/CLAUDE.md`).
- `flutter analyze` 기준선은 **50 issues** (error 0). 이 숫자가 늘면 안 된다.

---

### Task 1: 피드 모델 + 파서

**Files:**
- Create: `lib/service/map/models/map_feed.dart`
- Create: `test/service/map/models/map_feed_test.dart`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `enum MapFeedSectionType { latest, hot, nearby }`
  - `class MapFeedItem` — 필드: `boardId String`, `thumbnailUrl String?`, `title String`, `content String`, `streetName String`, `dong String?`, `latitude double`, `longitude double`, `nickname String`, `profileImage String?`, `likeCount int`, `replyCount int`, `createdAt String`
  - `class MapFeedSection` — `type MapFeedSectionType`, `title String`, `hasMore bool`, `items List<MapFeedItem>`
  - `class MapFeedResponse` — `dong String?`, `sections List<MapFeedSection>`
  - `MapFeedResponse? decodeMapFeedResponse(http.Response response)`

- [ ] **Step 1: 실패하는 테스트 작성**

`test/service/map/models/map_feed_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:http/http.dart' as http;
import 'package:unimal/service/map/models/map_feed.dart';

const _fullJson = '''
{"code":200,"message":"Success","data":{
  "dong":"역삼동",
  "sections":[
    {"type":"LATEST","title":"방금 올라온 소식","hasMore":true,"items":[
      {"boardId":"aB3xY9","thumbnailUrl":"https://cdn.example/a.jpg",
       "title":"역삼동 골목 고양이","content":"퇴근길에 만난 삼색이",
       "streetName":"서울 강남구 역삼로 123","dong":"역삼동",
       "latitude":37.5006,"longitude":127.0366,
       "nickname":"대영","profileImage":"https://cdn.example/p.jpg",
       "likeCount":12,"replyCount":3,"createdAt":"2026-07-29T14:33:00"}
    ]},
    {"type":"HOT","title":"지금 인기 있는 스토리","hasMore":false,"items":[]}
  ]}}
''';

void main() {
  test('camelCase 키를 정상 파싱한다', () {
    final result = decodeMapFeedResponse(http.Response(_fullJson, 200));

    expect(result, isNotNull);
    expect(result!.dong, '역삼동');
    expect(result.sections.length, 2);

    final latest = result.sections.first;
    expect(latest.type, MapFeedSectionType.latest);
    expect(latest.title, '방금 올라온 소식');
    expect(latest.hasMore, isTrue);
    expect(latest.items.length, 1);

    final item = latest.items.first;
    expect(item.boardId, 'aB3xY9');
    expect(item.thumbnailUrl, 'https://cdn.example/a.jpg');
    expect(item.title, '역삼동 골목 고양이');
    expect(item.nickname, '대영');
    expect(item.profileImage, 'https://cdn.example/p.jpg');
    expect(item.streetName, '서울 강남구 역삼로 123');
    expect(item.latitude, 37.5006);
    expect(item.longitude, 127.0366);
    expect(item.likeCount, 12);
    expect(item.replyCount, 3);
    expect(item.createdAt, '2026-07-29T14:33:00');
  });

  test('모르는 섹션 타입은 그 섹션만 건너뛴다', () {
    const json = '''
    {"data":{"dong":null,"sections":[
      {"type":"SOMETHING_NEW","title":"미래 섹션","hasMore":false,"items":[]},
      {"type":"NEARBY","title":"역삼동 이웃들의 스토리","hasMore":true,"items":[]}
    ]}}
    ''';

    final result = decodeMapFeedResponse(http.Response(json, 200));

    expect(result, isNotNull);
    expect(result!.sections.length, 1);
    expect(result.sections.single.type, MapFeedSectionType.nearby);
  });

  test('nullable 필드 누락을 견딘다', () {
    const json = '''
    {"data":{"sections":[
      {"type":"LATEST","title":"방금 올라온 소식","hasMore":false,"items":[
        {"boardId":"x1","title":"","content":"사진 없는 글",
         "streetName":"","latitude":37.5,"longitude":127.0,
         "nickname":"익명","likeCount":0,"replyCount":0,
         "createdAt":"2026-07-29T10:00:00"}
      ]}
    ]}}
    ''';

    final result = decodeMapFeedResponse(http.Response(json, 200));

    expect(result, isNotNull);
    expect(result!.dong, isNull);
    final item = result.sections.single.items.single;
    expect(item.thumbnailUrl, isNull);
    expect(item.profileImage, isNull);
    expect(item.dong, isNull);
    expect(item.content, '사진 없는 글');
  });

  test('HTTP 실패는 null이다', () {
    expect(
      decodeMapFeedResponse(http.Response('{"message":"error"}', 500)),
      isNull,
    );
  });

  test('잘못된 JSON은 null이다', () {
    expect(decodeMapFeedResponse(http.Response('{not-json}', 200)), isNull);
  });

  test('data가 없으면 null이다', () {
    expect(decodeMapFeedResponse(http.Response('{"code":200}', 200)), isNull);
  });
}
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `flutter test test/service/map/models/map_feed_test.dart`
Expected: 컴파일 실패 — `Target of URI doesn't exist: 'package:unimal/service/map/models/map_feed.dart'`

- [ ] **Step 3: 모델 구현**

`lib/service/map/models/map_feed.dart`:

```dart
import 'dart:convert';

import 'package:http/http.dart' as http;

/// 지도 바텀카드 피드 섹션 종류.
///
/// 서버는 앱 배포 없이 섹션을 추가할 수 있어야 한다(헤더 문구도 서버가 내려준다).
/// 그래서 파싱 단계에서 **모르는 타입은 그 섹션만 버린다** — 이 enum 에 unknown
/// 멤버를 두지 않는 이유다. UI 까지 unknown 이 흘러가면 화면에서 또 방어해야 한다.
enum MapFeedSectionType {
  latest,
  hot,
  nearby;

  /// 서버 문자열 → enum. 모르는 값이면 null (호출자가 그 섹션을 건너뛴다).
  static MapFeedSectionType? tryParse(String? raw) {
    switch (raw) {
      case 'LATEST':
        return MapFeedSectionType.latest;
      case 'HOT':
        return MapFeedSectionType.hot;
      case 'NEARBY':
        return MapFeedSectionType.nearby;
      default:
        return null;
    }
  }
}

/// 피드 카드 1장.
///
/// **이 API는 camelCase 다.** 앱에서 snake_case 를 쓰는 모델은 `MapPost` 뿐이므로
/// "지도 피드니까" `MapPost.fromJson` 을 참고하면 전부 null 이 된다.
///
/// 서버가 `isLike`/`isOwner`/`score`/전체 이미지 목록을 일부러 뺐다 — 응답이
/// 사용자에 의존하지 않아야 캐시가 가능하기 때문이다. 그래서 카드 탭 시에는
/// `getBoardDetail` 로 상세를 다시 받아야 한다 (설계 §5).
class MapFeedItem {
  final String boardId;

  /// 마커/카드용 썸네일. 사진 없는 텍스트 글이거나 썸네일 파생 실패 시 null.
  final String? thumbnailUrl;
  final String title;
  final String content;
  final String streetName;
  final String? dong;
  final double latitude;
  final double longitude;
  final String nickname;
  final String? profileImage;
  final int likeCount;
  final int replyCount;
  final String createdAt;

  const MapFeedItem({
    required this.boardId,
    this.thumbnailUrl,
    required this.title,
    required this.content,
    required this.streetName,
    this.dong,
    required this.latitude,
    required this.longitude,
    required this.nickname,
    this.profileImage,
    required this.likeCount,
    required this.replyCount,
    required this.createdAt,
  });

  factory MapFeedItem.fromJson(Map<String, dynamic> json) {
    return MapFeedItem(
      boardId: json['boardId'] as String? ?? '',
      thumbnailUrl: json['thumbnailUrl'] as String?,
      title: json['title'] as String? ?? '',
      content: json['content'] as String? ?? '',
      streetName: json['streetName'] as String? ?? '',
      dong: json['dong'] as String?,
      latitude: (json['latitude'] as num?)?.toDouble() ?? 0.0,
      longitude: (json['longitude'] as num?)?.toDouble() ?? 0.0,
      nickname: json['nickname'] as String? ?? '',
      profileImage: json['profileImage'] as String?,
      likeCount: (json['likeCount'] as num?)?.toInt() ?? 0,
      replyCount: (json['replyCount'] as num?)?.toInt() ?? 0,
      createdAt: json['createdAt'] as String? ?? '',
    );
  }
}

/// 섹션 1개. 서버가 3건 미만 섹션을 아예 제외하고 섹션 간 중복도 제거해 내려준다.
/// 앱은 받은 섹션을 순서대로 그리기만 한다 — 분기 로직을 클라이언트에 두지 않는다.
class MapFeedSection {
  final MapFeedSectionType type;

  /// 헤더 문구. 서버가 조립해 내려준다(`NEARBY` 는 동 이름이 들어간다).
  /// 앱에 하드코딩하지 않는 이유: 앱 배포 없이 문구를 바꿀 수 있어야 한다.
  final String title;
  final bool hasMore;
  final List<MapFeedItem> items;

  const MapFeedSection({
    required this.type,
    required this.title,
    required this.hasMore,
    required this.items,
  });

  /// 모르는 `type` 이면 null — 호출자가 이 섹션을 버린다.
  static MapFeedSection? tryFromJson(Map<String, dynamic> json) {
    final type = MapFeedSectionType.tryParse(json['type'] as String?);
    if (type == null) return null;
    final rawItems = json['items'];
    return MapFeedSection(
      type: type,
      title: json['title'] as String? ?? '',
      hasMore: json['hasMore'] as bool? ?? false,
      items: rawItems is List
          ? rawItems
              .whereType<Map<String, dynamic>>()
              .map(MapFeedItem.fromJson)
              .toList()
          : const <MapFeedItem>[],
    );
  }
}

class MapFeedResponse {
  /// 현재 행정동. 서버가 파라미터 또는 역지오코딩으로 확정한 값.
  /// `NEARBY` 섹션이 제외된 경우 null 일 수 있다.
  final String? dong;
  final List<MapFeedSection> sections;

  const MapFeedResponse({this.dong, required this.sections});

  factory MapFeedResponse.fromJson(Map<String, dynamic> json) {
    final rawSections = json['sections'];
    return MapFeedResponse(
      dong: json['dong'] as String?,
      sections: rawSections is List
          ? rawSections
              .whereType<Map<String, dynamic>>()
              .map(MapFeedSection.tryFromJson)
              .whereType<MapFeedSection>() // 모르는 타입 섹션 제거
              .toList()
          : const <MapFeedSection>[],
    );
  }
}

/// HTTP 응답 → 피드. 실패/파싱불가면 null.
/// `decodeMapPostsResponse`(board_api_service.dart)와 같은 형태 — HTTP 없이
/// 테스트할 수 있도록 최상위 함수로 둔다.
MapFeedResponse? decodeMapFeedResponse(http.Response response) {
  if (response.statusCode != 200) return null;
  try {
    final body = jsonDecode(utf8.decode(response.bodyBytes));
    if (body is! Map) return null;
    final data = body['data'];
    if (data is! Map<String, dynamic>) return null;
    return MapFeedResponse.fromJson(data);
  } catch (_) {
    return null;
  }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `flutter test test/service/map/models/map_feed_test.dart`
Expected: 6개 테스트 PASS

- [ ] **Step 5: 정적 분석**

Run: `flutter analyze`
Expected: `50 issues found.` (error 0, 기준선과 동일)

---

### Task 2: API 서비스 + 목 데이터

**Files:**
- Create: `lib/service/map/map_feed_mock.dart`
- Modify: `lib/service/board/board_api_service.dart` (파일 끝 `getMyLikedPostList` 뒤에 메서드 추가)
- Modify: `.env.local` (키 1줄 추가)

**Interfaces:**
- Consumes: Task 1 의 `decodeMapFeedResponse`, `MapFeedResponse`
- Produces:
  - `BoardApiService.getMapFeed({required double latitude, required double longitude, required int zoom}) → Future<MapFeedResponse?>`
  - `const String kMapFeedMockJson`

- [ ] **Step 1: 목 JSON 파일 생성**

`lib/service/map/map_feed_mock.dart`:

```dart
/// 서버 미구현 기간 UI 확인용 목 응답.
///
/// **이 문자열은 실제 파서(`decodeMapFeedResponse`)를 그대로 탄다.** 목이 파서를
/// 우회하면 목으로 본 화면과 실서버 화면이 달라질 수 있다.
///
/// 활성 조건: `.env.local` 의 `MAP_FEED_MOCK=true`. 릴리즈는 `.env.prod` 를 쓰고
/// 그 파일에는 키가 없으므로 자동으로 비활성이다.
///
/// 서버가 붙으면: `.env.local` 에서 키를 지우고 이 파일과
/// `board_api_service.getMapFeed` 의 분기 3줄을 삭제한다.
///
/// 데이터 구성 의도:
/// - `LATEST` 5건 — 사진 글 4 + **사진 없는 텍스트 글 1**(`thumbnailUrl` null 렌더 확인)
/// - `HOT` 3건 — `hasMore: false` (헤더에 `>` 안 뜨는지 확인)
/// - `NEARBY` 4건 — `hasMore: true`
/// - 제목 빈 글 1건 포함 (본문 폴백 확인)
const String kMapFeedMockJson = '''
{
  "code": 200,
  "message": "Success",
  "data": {
    "dong": "역삼동",
    "sections": [
      {
        "type": "LATEST",
        "title": "방금 올라온 소식",
        "hasMore": true,
        "items": [
          {
            "boardId": "mock-l1",
            "thumbnailUrl": "https://cdn.unimal.co.kr/images/c2NhbGVkX0lNR18yMDI2MDQwN18xNjMxMzAuanBn-693be74f7721479bb08a055c67888b5c.jpeg",
            "title": "역삼동 골목 고양이",
            "content": "퇴근길에 만난 삼색이. 사람을 안 피하네요.",
            "streetName": "서울 강남구 역삼로 123",
            "dong": "역삼동",
            "latitude": 37.5006,
            "longitude": 127.0366,
            "nickname": "대영",
            "profileImage": null,
            "likeCount": 12,
            "replyCount": 3,
            "createdAt": "2026-07-29T14:33:00"
          },
          {
            "boardId": "mock-l2",
            "thumbnailUrl": "https://cdn.unimal.co.kr/images/c2NhbGVkXzIwMjYwNjMwXzIzMTMzMi5qcGc=-d48c4a41ed1f46fc96b4bc26d1acdc72.jpeg",
            "title": "",
            "content": "제목 없는 글입니다. 카드에서 본문이 제목 자리에 와야 합니다.",
            "streetName": "서울 강남구 테헤란로 44",
            "dong": "역삼동",
            "latitude": 37.5012,
            "longitude": 127.0372,
            "nickname": "지수",
            "profileImage": null,
            "likeCount": 4,
            "replyCount": 0,
            "createdAt": "2026-07-29T13:10:00"
          },
          {
            "boardId": "mock-l3",
            "thumbnailUrl": null,
            "title": "사진 없는 글",
            "content": "썸네일이 없을 때 카드가 어떻게 보이는지 확인용입니다.",
            "streetName": "서울 강남구 봉은사로 12",
            "dong": "역삼동",
            "latitude": 37.5031,
            "longitude": 127.0341,
            "nickname": "민호",
            "profileImage": null,
            "likeCount": 1,
            "replyCount": 1,
            "createdAt": "2026-07-29T11:05:00"
          },
          {
            "boardId": "mock-l4",
            "thumbnailUrl": "https://cdn.unimal.co.kr/images/aW1hZ2VfcGlja2VyX0U1MDg3MDJCLTIyOUUtNDQ1Qi04MEY2LUQ0RUE0RUM3NjcwNy0zODQ4LTAwMDAwMUVBODE3RDIzRDkuanBn-625234aa73ac4ee2a6b4a7478eb9d258.jpeg",
            "title": "점심 맛집 발견",
            "content": "줄 서서 먹을 만합니다.",
            "streetName": "서울 강남구 역삼로 87",
            "dong": "역삼동",
            "latitude": 37.4998,
            "longitude": 127.0388,
            "nickname": "현우",
            "profileImage": null,
            "likeCount": 22,
            "replyCount": 7,
            "createdAt": "2026-07-29T09:40:00"
          },
          {
            "boardId": "mock-l5",
            "thumbnailUrl": "https://cdn.unimal.co.kr/images/MzE3YzIzMmQtMTIzMC00YjM2LTliZjAtZjAzMDA1YWZmOTI5NzY2MTc1MjY0MzIxNjA3MjMyNS5qcGc=-3c507125752c481fbc021a245ecd7853.jpeg",
            "title": "한강 야경",
            "content": "오늘 바람이 좋았어요.",
            "streetName": "서울 강남구 압구정로 1",
            "dong": "역삼동",
            "latitude": 37.5104,
            "longitude": 127.0301,
            "nickname": "서연",
            "profileImage": null,
            "likeCount": 31,
            "replyCount": 5,
            "createdAt": "2026-07-28T21:15:00"
          }
        ]
      },
      {
        "type": "HOT",
        "title": "지금 인기 있는 스토리",
        "hasMore": false,
        "items": [
          {
            "boardId": "mock-h1",
            "thumbnailUrl": "https://cdn.unimal.co.kr/images/aW1hZ2VfcGlja2VyX0EyRUQ4NDFDLTdCNzYtNEIyMi04NUNGLUFBNTA5RUYzMkJDMS05NTYwLTAwMDAwNTAwOUI1QzNGMUYuanBn-3e1c70eee0234c238bd9f5726bee62df.jpeg",
            "title": "동네 벽화 완성",
            "content": "주말에 다 같이 칠했습니다.",
            "streetName": "서울 강남구 언주로 30",
            "dong": "역삼동",
            "latitude": 37.4971,
            "longitude": 127.0333,
            "nickname": "가영",
            "profileImage": null,
            "likeCount": 88,
            "replyCount": 24,
            "createdAt": "2026-07-27T18:00:00"
          },
          {
            "boardId": "mock-h2",
            "thumbnailUrl": "https://cdn.unimal.co.kr/images/aW1hZ2VfcGlja2VyXzJBRUQwRTc4LThGREUtNEIyQy1CQjkzLUQxREEyRURFQzgwNS05NTYwLTAwMDAwNTAwQTEyMDc1M0MuanBn-1d97278c7f724f4b9fb0d1929020b4f3.jpeg",
            "title": "길 잃은 강아지 찾았어요",
            "content": "주인분 찾습니다.",
            "streetName": "서울 강남구 도곡로 5",
            "dong": "역삼동",
            "latitude": 37.4955,
            "longitude": 127.0359,
            "nickname": "준영",
            "profileImage": null,
            "likeCount": 54,
            "replyCount": 19,
            "createdAt": "2026-07-27T12:30:00"
          },
          {
            "boardId": "mock-h3",
            "thumbnailUrl": "https://cdn.unimal.co.kr/images/aW1hZ2VfcGlja2VyXzEyNEU5REUyLTlENkYtNDM3Mi04MUUwLTY2OThFQzBFNDc3NS05ODE4LTAwMDAwNTJBQUU5N0FEQjQuanBn-0308bd60cf444e099cb48f1dbd665916.jpeg",
            "title": "플리마켓 후기",
            "content": "생각보다 사람이 많았습니다.",
            "streetName": "서울 강남구 선릉로 100",
            "dong": "역삼동",
            "latitude": 37.5044,
            "longitude": 127.0489,
            "nickname": "하늘",
            "profileImage": null,
            "likeCount": 40,
            "replyCount": 11,
            "createdAt": "2026-07-26T16:20:00"
          }
        ]
      },
      {
        "type": "NEARBY",
        "title": "역삼동 이웃들의 스토리",
        "hasMore": true,
        "items": [
          {
            "boardId": "mock-n1",
            "thumbnailUrl": "https://cdn.unimal.co.kr/images/aW1hZ2VfcGlja2VyXzU0QzZGMTE5LTgxMjMtNDQxNy05RDM3LTI0MDg2REM4Q0QxNS05ODE4LTAwMDAwNTJBQUVEQkNBRTAuanBn-f1116ab83eef470ebe0084ca47d8054d.jpeg",
            "title": "새로 생긴 카페",
            "content": "조용해서 작업하기 좋아요.",
            "streetName": "서울 강남구 역삼로 200",
            "dong": "역삼동",
            "latitude": 37.5021,
            "longitude": 127.0412,
            "nickname": "다온",
            "profileImage": null,
            "likeCount": 9,
            "replyCount": 2,
            "createdAt": "2026-07-25T10:00:00"
          },
          {
            "boardId": "mock-n2",
            "thumbnailUrl": "https://cdn.unimal.co.kr/images/aW1hZ2VfcGlja2VyX0U2MzMxNDBBLURBMjItNDdENi04RTdCLTA0QTJENTQzNjNCOC0xMDM3Ny0wMDAwMDU0RTJGQ0E5ODNDLmpwZw==-834210a55e5f4d479dd91894b98565a6.jpeg",
            "title": "공원 산책로 정비 끝",
            "content": "이제 걷기 편합니다.",
            "streetName": "서울 강남구 논현로 77",
            "dong": "역삼동",
            "latitude": 37.4989,
            "longitude": 127.0299,
            "nickname": "은지",
            "profileImage": null,
            "likeCount": 6,
            "replyCount": 0,
            "createdAt": "2026-07-24T08:45:00"
          },
          {
            "boardId": "mock-n3",
            "thumbnailUrl": "https://cdn.unimal.co.kr/images/aW1hZ2VfcGlja2VyXzkyMjhDNjM3LUEzODctNDkyNS05NUEyLTZDNDVFOTM3RDdFMi0xMDM3Ny0wMDAwMDU0RTMwNUFGMUQ1LmpwZw==-b608c9c98233494fa46e49c280e36a0b.jpeg",
            "title": "중고 책장 나눔",
            "content": "가져가실 분 댓글 주세요.",
            "streetName": "서울 강남구 역삼로 55",
            "dong": "역삼동",
            "latitude": 37.5002,
            "longitude": 127.0350,
            "nickname": "태현",
            "profileImage": null,
            "likeCount": 3,
            "replyCount": 8,
            "createdAt": "2026-07-23T19:30:00"
          },
          {
            "boardId": "mock-n4",
            "thumbnailUrl": "https://cdn.unimal.co.kr/images/aW1hZ2VfcGlja2VyXzFCNTg2QzFELUIzMTQtNDY0QS04RDg3LURGNkE5RjA5ODQzNC0xMDM3Ny0wMDAwMDU0RTMwMTIxREM5LmpwZw==-ee3d3d698f2e4a39a19f3f6d34bc6fbb.jpeg",
            "title": "골목 벼룩시장",
            "content": "토요일마다 열립니다.",
            "streetName": "서울 강남구 테헤란로 200",
            "dong": "역삼동",
            "latitude": 37.5038,
            "longitude": 127.0441,
            "nickname": "소라",
            "profileImage": null,
            "likeCount": 15,
            "replyCount": 4,
            "createdAt": "2026-07-22T14:00:00"
          }
        ]
      }
    ]
  }
}
''';
```

- [ ] **Step 2: `.env.local` 에 플래그 추가**

`.env.local` 파일 끝에 한 줄 추가:

```
MAP_FEED_MOCK=true
```

**`.env.prod` 는 건드리지 않는다** — 키가 없으면 자동으로 비활성이다.

- [ ] **Step 3: 서비스 메서드 추가**

`lib/service/board/board_api_service.dart` — import 2줄 추가:

```dart
import 'package:flutter_dotenv/flutter_dotenv.dart';
import 'package:unimal/service/map/map_feed_mock.dart';
import 'package:unimal/service/map/models/map_feed.dart';
```

`BoardApiService` 클래스 안, `getMyLikedPostList` 메서드 뒤에 추가:

```dart
  // ── 지도 바텀카드 피드 ──────────────────────────────────────────────
  /// 섹션 피드 조회. 실패/파싱불가면 null (호출자가 시트를 숨긴다).
  ///
  /// 서버가 3건 미만 섹션 제외·섹션 간 중복 제거까지 해서 내려주므로 앱은
  /// 받은 섹션을 그대로 그린다.
  ///
  /// 주소(`siDo`/`guGun`/`dong`)는 넘기지 않는다 — 지도 화면이 현재 동을 모르고,
  /// 앱이 역지오코딩을 먼저 하면 순차 2 RTT 가 되어 이 API 의 존재 이유(1 RTT)가
  /// 깨진다. 서버가 gRPC 역지오코딩 폴백 + 캐시를 갖고 있다.
  Future<MapFeedResponse?> getMapFeed({
    required double latitude,
    required double longitude,
    required int zoom,
  }) async {
    // 서버 미구현 기간 UI 확인용 (map_feed_mock.dart 참고).
    // 서버가 붙으면 이 분기와 목 파일을 삭제한다.
    //
    // `http.Response(문자열, 200)` 을 쓰면 안 된다 — headers 가 없으면
    // package:http 가 body 를 **latin1** 으로 인코딩하고(`utils.dart`
    // `_encodingForHeaders` 의 fallback), 목 JSON 의 한글이 인코딩 불가라
    // FormatException 을 던진다. 이 분기는 아래 try 밖이라 잡히지도 않는다.
    // 반드시 `Response.bytes(utf8.encode(...))` 로 바이트를 직접 넣는다.
    if (dotenv.env['MAP_FEED_MOCK']?.toLowerCase() == 'true') {
      return decodeMapFeedResponse(
        http.Response.bytes(utf8.encode(kMapFeedMockJson), 200),
      );
    }

    try {
      final url = ApiUri.resolve('board/map/feed', {
        'latitude': latitude.toString(),
        'longitude': longitude.toString(),
        'zoom': zoom.toString(),
      });
      final headers = await _authHeaders();
      final response = await ApiClient.get(url, headers);
      final feed = decodeMapFeedResponse(response);
      if (feed == null) {
        _logger.e('지도 피드 조회 실패: ${response.statusCode}');
      }
      return feed;
    } catch (e, st) {
      _logger.e('지도 피드 조회 예외', error: e, stackTrace: st);
      return null;
    }
  }
```

- [ ] **Step 4: 정적 분석**

Run: `flutter analyze`
Expected: `50 issues found.` (error 0)

- [ ] **Step 5: 목이 실제 파서를 통과하는지 확인하는 테스트 추가**

`test/service/map/models/map_feed_test.dart` 파일 끝 `}` 앞에 추가:

```dart
  test('목 JSON은 실제 파서를 통과하고 3섹션을 준다', () {
    // Response.bytes + utf8.encode — 한글 body 는 기본 latin1 인코딩으로
    // 만들 수 없다 (Task 1 에서 확인된 제약, 같은 파일 다른 테스트와 동일 방식).
    final result = decodeMapFeedResponse(
      http.Response.bytes(utf8.encode(kMapFeedMockJson), 200),
    );

    expect(result, isNotNull);
    expect(result!.dong, '역삼동');
    expect(
      result.sections.map((s) => s.type).toList(),
      [
        MapFeedSectionType.latest,
        MapFeedSectionType.hot,
        MapFeedSectionType.nearby,
      ],
    );
    // 썸네일 없는 카드가 렌더 확인용으로 최소 1건 있어야 한다.
    final allItems = result.sections.expand((s) => s.items);
    expect(allItems.any((i) => i.thumbnailUrl == null), isTrue);
    // 제목 없는 카드도 1건 (본문 폴백 확인용).
    expect(allItems.any((i) => i.title.isEmpty), isTrue);
  });
```

같은 파일 import 에 추가 (`utf8` 용 `dart:convert` 포함):

```dart
import 'dart:convert';

import 'package:unimal/service/map/map_feed_mock.dart';
```

- [ ] **Step 6: 테스트 통과 확인**

Run: `flutter test test/service/map/models/map_feed_test.dart`
Expected: 7개 테스트 PASS

---

### Task 3: 피드 카드 + 섹션 행 위젯

**Files:**
- Create: `lib/screens/map/feed/map_feed_card.dart`
- Create: `lib/screens/map/feed/map_feed_section_row.dart`
- Create: `test/screens/map/feed/map_feed_section_row_test.dart`

**Interfaces:**
- Consumes: Task 1 의 `MapFeedItem`, `MapFeedSection`
- Produces:
  - `class MapFeedCard extends StatelessWidget` — `{required MapFeedItem item, required VoidCallback onTap}`
  - `const double kMapFeedCardWidth = 116` (시트가 카루셀 높이 계산에 사용)
  - `const double kMapFeedRowHeight = 178`
  - `class MapFeedSectionRow extends StatelessWidget` — `{required MapFeedSection section, required void Function(MapFeedItem) onItemTap}`

- [ ] **Step 1: 카드 위젯 구현**

`lib/screens/map/feed/map_feed_card.dart`:

```dart
import 'package:cached_network_image/cached_network_image.dart';
import 'package:flutter/material.dart';
import 'package:unimal/screens/map/bottom_card/relative_time.dart';
import 'package:unimal/service/map/models/map_feed.dart';
import 'package:unimal/theme/app_colors.dart';
import 'package:unimal/utils/display_title.dart';

/// 피드 카드 1장의 폭. 섹션 행 높이 계산과 함께 맞춰야 하므로 상수로 둔다.
const double kMapFeedCardWidth = 116;

/// 썸네일 정사각 변 = 카드 폭. 그 아래 제목 2줄 + 메타 1줄.
const double _thumbSize = kMapFeedCardWidth;

class MapFeedCard extends StatelessWidget {
  const MapFeedCard({super.key, required this.item, required this.onTap});

  final MapFeedItem item;
  final VoidCallback onTap;

  @override
  Widget build(BuildContext context) {
    final colors = AppColors.of(context);
    // 제목이 비면 본문 첫 줄을 제목 자리에 쓴다 (마커 캡션과 같은 규칙).
    final label = displayTitle(item.title, item.content);

    return GestureDetector(
      onTap: onTap,
      behavior: HitTestBehavior.opaque,
      child: SizedBox(
        width: kMapFeedCardWidth,
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            ClipRRect(
              borderRadius: BorderRadius.circular(14),
              child: _Thumbnail(item: item, colors: colors),
            ),
            const SizedBox(height: 6),
            Text(
              label,
              maxLines: 2,
              overflow: TextOverflow.ellipsis,
              style: TextStyle(
                fontSize: 12.5,
                height: 1.25,
                fontFamily: 'Pretendard',
                fontWeight: FontWeight.w600,
                color: colors.textPrimary,
              ),
            ),
            const SizedBox(height: 3),
            Row(
              children: [
                Icon(Icons.favorite, size: 11, color: colors.textMuted),
                const SizedBox(width: 2),
                Text(
                  '${item.likeCount}',
                  style: TextStyle(
                    fontSize: 11,
                    fontFamily: 'Pretendard',
                    color: colors.textMuted,
                  ),
                ),
                const SizedBox(width: 6),
                Expanded(
                  child: Text(
                    relativeTimeFromString(item.createdAt),
                    maxLines: 1,
                    overflow: TextOverflow.ellipsis,
                    style: TextStyle(
                      fontSize: 11,
                      fontFamily: 'Pretendard',
                      color: colors.textMuted,
                    ),
                  ),
                ),
              ],
            ),
          ],
        ),
      ),
    );
  }
}

/// 썸네일 — 없으면(텍스트 글 / 파생 실패) 본문 스니펫 타일로 대체한다.
class _Thumbnail extends StatelessWidget {
  const _Thumbnail({required this.item, required this.colors});

  final MapFeedItem item;
  final AppColors colors;

  @override
  Widget build(BuildContext context) {
    final url = item.thumbnailUrl;
    if (url == null || url.isEmpty) {
      return Container(
        width: _thumbSize,
        height: _thumbSize,
        color: colors.surfaceMuted,
        padding: const EdgeInsets.all(10),
        alignment: Alignment.topLeft,
        child: Text(
          item.content,
          maxLines: 5,
          overflow: TextOverflow.ellipsis,
          style: TextStyle(
            fontSize: 11,
            height: 1.35,
            fontFamily: 'Pretendard',
            color: colors.textSecondary,
          ),
        ),
      );
    }
    return CachedNetworkImage(
      imageUrl: url,
      width: _thumbSize,
      height: _thumbSize,
      fit: BoxFit.cover,
      placeholder: (_, __) => Container(
        width: _thumbSize,
        height: _thumbSize,
        color: colors.surfaceVariant,
      ),
      errorWidget: (_, __, ___) => Container(
        width: _thumbSize,
        height: _thumbSize,
        color: colors.surfaceVariant,
        alignment: Alignment.center,
        child: Icon(Icons.image_not_supported_outlined,
            size: 20, color: colors.textMuted),
      ),
    );
  }
}
```

- [ ] **Step 2: 섹션 행 위젯 구현**

`lib/screens/map/feed/map_feed_section_row.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:unimal/screens/map/feed/map_feed_card.dart';
import 'package:unimal/service/map/models/map_feed.dart';
import 'package:unimal/theme/app_colors.dart';

/// 섹션 행 전체 높이 = 썸네일(116) + 간격(6) + 제목 2줄(≈32) + 간격(3) + 메타(≈14)
/// + 하단 여유. 카루셀을 `SizedBox` 로 감싸야 `ListView` 안에서 높이가 확정된다.
const double kMapFeedRowHeight = 178;

/// 섹션 1개 = 헤더(서버가 준 title) + 가로 카루셀.
///
/// `hasMore` 면 헤더에 `>` 만 표시한다. **탭 동작은 없다** — 섹션별 더보기
/// 페이지네이션은 서버 스펙에서도 범위 밖이다.
class MapFeedSectionRow extends StatelessWidget {
  const MapFeedSectionRow({
    super.key,
    required this.section,
    required this.onItemTap,
  });

  final MapFeedSection section;
  final void Function(MapFeedItem item) onItemTap;

  @override
  Widget build(BuildContext context) {
    final colors = AppColors.of(context);

    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Padding(
          padding: const EdgeInsets.fromLTRB(16, 0, 16, 8),
          child: Row(
            children: [
              Expanded(
                child: Text(
                  section.title,
                  maxLines: 1,
                  overflow: TextOverflow.ellipsis,
                  style: TextStyle(
                    fontSize: 15,
                    fontFamily: 'Pretendard',
                    fontWeight: FontWeight.w700,
                    color: colors.textPrimary,
                  ),
                ),
              ),
              if (section.hasMore)
                Icon(Icons.chevron_right,
                    size: 20, color: colors.textMuted),
            ],
          ),
        ),
        SizedBox(
          height: kMapFeedRowHeight,
          child: ListView.separated(
            scrollDirection: Axis.horizontal,
            padding: const EdgeInsets.symmetric(horizontal: 16),
            itemCount: section.items.length,
            separatorBuilder: (_, __) => const SizedBox(width: 10),
            itemBuilder: (_, i) {
              final item = section.items[i];
              return MapFeedCard(
                item: item,
                onTap: () => onItemTap(item),
              );
            },
          ),
        ),
      ],
    );
  }
}
```

- [ ] **Step 3: 위젯 테스트 작성**

`test/screens/map/feed/map_feed_section_row_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:unimal/screens/map/feed/map_feed_card.dart';
import 'package:unimal/screens/map/feed/map_feed_section_row.dart';
import 'package:unimal/service/map/models/map_feed.dart';

MapFeedItem _item(String id, {String? thumb, String title = '제목'}) => MapFeedItem(
      boardId: id,
      thumbnailUrl: thumb,
      title: title,
      content: '본문 내용',
      streetName: '서울 강남구',
      latitude: 37.5,
      longitude: 127.0,
      nickname: '닉',
      likeCount: 3,
      replyCount: 1,
      createdAt: '2026-07-29T10:00:00',
    );

MapFeedSection _section({required bool hasMore, int count = 2}) => MapFeedSection(
      type: MapFeedSectionType.latest,
      title: '방금 올라온 소식',
      hasMore: hasMore,
      items: [for (int i = 0; i < count; i++) _item('id$i')],
    );

Widget _wrap(Widget child) => MaterialApp(
      home: Scaffold(body: SingleChildScrollView(child: child)),
    );

void main() {
  testWidgets('헤더 제목과 카드를 그린다', (tester) async {
    await tester.pumpWidget(_wrap(MapFeedSectionRow(
      section: _section(hasMore: false),
      onItemTap: (_) {},
    )));

    expect(find.text('방금 올라온 소식'), findsOneWidget);
    expect(find.byType(MapFeedCard), findsNWidgets(2));
  });

  testWidgets('hasMore가 false면 화살표를 숨긴다', (tester) async {
    await tester.pumpWidget(_wrap(MapFeedSectionRow(
      section: _section(hasMore: false),
      onItemTap: (_) {},
    )));

    expect(find.byIcon(Icons.chevron_right), findsNothing);
  });

  testWidgets('hasMore가 true면 화살표를 보인다', (tester) async {
    await tester.pumpWidget(_wrap(MapFeedSectionRow(
      section: _section(hasMore: true),
      onItemTap: (_) {},
    )));

    expect(find.byIcon(Icons.chevron_right), findsOneWidget);
  });

  testWidgets('카드를 탭하면 해당 아이템으로 콜백한다', (tester) async {
    MapFeedItem? tapped;
    await tester.pumpWidget(_wrap(MapFeedSectionRow(
      section: _section(hasMore: false),
      onItemTap: (i) => tapped = i,
    )));

    await tester.tap(find.byType(MapFeedCard).first);
    await tester.pump();

    expect(tapped?.boardId, 'id0');
  });

  testWidgets('썸네일이 없으면 본문 스니펫을 보여준다', (tester) async {
    final section = MapFeedSection(
      type: MapFeedSectionType.latest,
      title: '방금 올라온 소식',
      hasMore: false,
      items: [_item('t1', thumb: null)],
    );

    await tester.pumpWidget(_wrap(MapFeedSectionRow(
      section: section,
      onItemTap: (_) {},
    )));

    // 제목 자리 + 썸네일 대체 타일에 각각 본문이 나올 수 있으므로 최소 1개.
    expect(find.text('본문 내용'), findsWidgets);
  });
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `flutter test test/screens/map/feed/map_feed_section_row_test.dart`
Expected: 5개 테스트 PASS

- [ ] **Step 5: 정적 분석**

Run: `flutter analyze`
Expected: `50 issues found.` (error 0)

---

### Task 4: 피드 시트

**Files:**
- Create: `lib/screens/map/feed/map_feed_sheet.dart`
- Create: `test/screens/map/feed/map_feed_sheet_test.dart`

**Interfaces:**
- Consumes: Task 1 의 `MapFeedResponse`/`MapFeedItem`, Task 2 의 `BoardApiService.getMapFeed`, Task 3 의 `MapFeedSectionRow`
- Produces:
  - `class MapFeedQuery` — `{double latitude, double longitude, int zoom}`, `==`/`hashCode` 구현
  - `class MapFeedSheet extends StatefulWidget` —
    `{required ValueListenable<MapFeedQuery?> query, required DraggableScrollableController controller, required void Function(MapFeedItem) onItemTap}`
  - `const double kMapFeedPeekSize = 0.15`

- [ ] **Step 1: 시트 구현**

`lib/screens/map/feed/map_feed_sheet.dart`:

```dart
import 'dart:async';

import 'package:flutter/foundation.dart';
import 'package:flutter/material.dart';
import 'package:unimal/screens/map/feed/map_feed_section_row.dart';
import 'package:unimal/service/board/board_api_service.dart';
import 'package:unimal/service/map/models/map_feed.dart';
import 'package:unimal/theme/app_colors.dart';

/// 시트 peek 크기 — 화면 높이 대비 비율. **하한이자 초기값이다.**
/// 사용자가 시트를 완전히 없앨 수는 없다(상시 노출이 이 기능의 전제).
const double kMapFeedPeekSize = 0.15;
const double _kMaxSize = 0.9;

/// "펼쳐져 있음" 판정 임계값. peek 보다 약간 크게 둬서 스냅 애니메이션 도중의
/// 중간값이 "펼쳐짐"으로 오판되지 않게 한다.
const double _kExpandedThreshold = kMapFeedPeekSize + 0.05;

/// 피드 조회 좌표/줌. `map_naver` 가 마커 재조회 성공 시점에 갱신한다.
///
/// `==` 를 구현해야 같은 좌표로 알림이 와도 중복 조회하지 않는다.
@immutable
class MapFeedQuery {
  const MapFeedQuery({
    required this.latitude,
    required this.longitude,
    required this.zoom,
  });

  final double latitude;
  final double longitude;
  final int zoom;

  @override
  bool operator ==(Object other) =>
      other is MapFeedQuery &&
      other.latitude == latitude &&
      other.longitude == longitude &&
      other.zoom == zoom;

  @override
  int get hashCode => Object.hash(latitude, longitude, zoom);
}

/// 메인 지도 하단 상시 피드 시트.
///
/// **데이터를 스스로 소유한다.** `map_naver.dart` 가 이미 2,900줄이라 상태를 더
/// 얹지 않기 위한 결정이다(설계 §3). `map_naver` 는 [query] 갱신과
/// [controller] 보유만 한다.
///
/// 섹션이 하나도 없으면(빈 결과·조회 실패·서버 미구현) **아무것도 렌더하지
/// 않는다.** peek 은 "올릴 게 있다"는 신호이므로 올릴 게 없으면 없는 것이 맞다.
class MapFeedSheet extends StatefulWidget {
  const MapFeedSheet({
    super.key,
    required this.query,
    required this.controller,
    required this.onItemTap,
  });

  final ValueListenable<MapFeedQuery?> query;

  /// 마커 탭 시 외부에서 접어야 하므로 컨트롤러는 부모가 소유한다.
  final DraggableScrollableController controller;

  final void Function(MapFeedItem item) onItemTap;

  @override
  State<MapFeedSheet> createState() => _MapFeedSheetState();
}

class _MapFeedSheetState extends State<MapFeedSheet> {
  MapFeedResponse? _feed;
  MapFeedQuery? _loadedQuery;
  bool _isLoading = false;

  @override
  void initState() {
    super.initState();
    widget.query.addListener(_onQueryChanged);
    // 화면 진입 시 이미 값이 들어와 있을 수 있다.
    _onQueryChanged();
  }

  @override
  void dispose() {
    widget.query.removeListener(_onQueryChanged);
    super.dispose();
  }

  /// 컨트롤러가 시트에 붙기 전에는 `size` 가 던진다 — 그 경우 접힌 것으로 본다.
  bool get _isExpanded {
    try {
      return widget.controller.size > _kExpandedThreshold;
    } catch (_) {
      return false;
    }
  }

  /// 아직 보여줄 내용이 없는 상태.
  ///
  /// 이때는 시트가 렌더되지 않아(`build` 가 `SizedBox.shrink`) 컨트롤러가 부착되지
  /// 않고, 따라서 [_isExpanded] 가 영원히 false 다. 즉 **사용자가 시트를 펼칠 수단
  /// 자체가 없다.** 그래서 내용이 없는 동안은 peek 게이트를 우회해 계속 조회한다.
  bool get _hasContent => _feed?.sections.isNotEmpty ?? false;

  void _onQueryChanged() {
    final query = widget.query.value;
    if (query == null) return;

    // 보여줄 내용이 없으면 펼침 여부와 무관하게 조회한다.
    //
    // ⚠️ 이 우회를 "첫 1회만" 으로 만들면(`_firstLoadDone` 같은 플래그) **첫 조회가
    // 실패하는 순간 피드가 영구히 죽는다**: 내용 없음 → 시트 미렌더 → 컨트롤러
    // 미부착 → _isExpanded 영원히 false → 이후 모든 조회가 게이트에 막힘. 서버
    // 미구현(목 플래그 off)에서는 반드시 발생하고, 서버가 붙은 뒤에도 진입 시
    // 일시적 네트워크 실패 한 번으로 재현된다 (2026-07-29 리뷰에서 발견).
    //
    // 조회 폭증 걱정은 없다 — 이 알림은 마커 재조회 성공 시에만 오고, 그 재조회는
    // 디바운스 300ms / 뷰포트 30% 이동 / 30초 신선도로 이미 조여져 있다.
    if (!_hasContent) {
      unawaited(_fetch(query));
      return;
    }
    // 내용이 생긴 뒤로는 시트가 펼쳐져 있을 때만 갱신한다 (설계 §3 재조회 시점).
    if (!_isExpanded) return;
    if (query == _loadedQuery) return;
    unawaited(_fetch(query));
  }

  Future<void> _fetch(MapFeedQuery query) async {
    if (_isLoading) return;
    setState(() => _isLoading = true);

    final feed = await BoardApiService().getMapFeed(
      latitude: query.latitude,
      longitude: query.longitude,
      zoom: query.zoom,
    );
    if (!mounted) return;

    setState(() {
      _isLoading = false;
      // 실패 시 기존 내용을 유지한다 — 깜빡임보다 묵은 데이터가 낫다.
      // 실패했다는 사실을 상태로 남기지 않는다 (위 _hasContent 주석 참고).
      if (feed != null) {
        _feed = feed;
        _loadedQuery = query;
      }
    });
  }

  @override
  Widget build(BuildContext context) {
    final sections = _feed?.sections ?? const <MapFeedSection>[];
    // 보여줄 게 없으면 시트 자체를 만들지 않는다.
    if (sections.isEmpty) return const SizedBox.shrink();

    final colors = AppColors.of(context);

    return DraggableScrollableSheet(
      controller: widget.controller,
      initialChildSize: kMapFeedPeekSize,
      minChildSize: kMapFeedPeekSize,
      maxChildSize: _kMaxSize,
      snap: true,
      snapSizes: const [kMapFeedPeekSize, _kMaxSize],
      builder: (context, scrollController) {
        return Container(
          decoration: BoxDecoration(
            color: colors.surface,
            borderRadius:
                const BorderRadius.vertical(top: Radius.circular(22)),
            boxShadow: [
              BoxShadow(
                color: colors.shadow,
                blurRadius: 16,
                offset: const Offset(0, -2),
              ),
            ],
          ),
          clipBehavior: Clip.antiAlias,
          // 핸들을 같은 스크롤뷰 안에 둬야 핸들을 잡아도 시트가 끌린다
          // (DraggableScrollableSheet 는 controller 가 붙은 스크롤러로만 끌림 —
          //  my_story_map_screen 의 같은 주석 참고).
          child: ListView(
            controller: scrollController,
            padding: EdgeInsets.zero,
            children: [
              const SizedBox(height: 10),
              Center(
                child: Container(
                  width: 40,
                  height: 4,
                  decoration: BoxDecoration(
                    color: colors.divider,
                    borderRadius: BorderRadius.circular(2),
                  ),
                ),
              ),
              const SizedBox(height: 14),
              for (final section in sections) ...[
                MapFeedSectionRow(
                  section: section,
                  onItemTap: widget.onItemTap,
                ),
                const SizedBox(height: 18),
              ],
              SizedBox(height: MediaQuery.paddingOf(context).bottom + 8),
            ],
          ),
        );
      },
    );
  }
}
```

- [ ] **Step 2: 위젯 테스트 작성**

`test/screens/map/feed/map_feed_sheet_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:unimal/screens/map/feed/map_feed_sheet.dart';

void main() {
  test('MapFeedQuery는 같은 값이면 동등하다', () {
    const a = MapFeedQuery(latitude: 37.5, longitude: 127.0, zoom: 16);
    const b = MapFeedQuery(latitude: 37.5, longitude: 127.0, zoom: 16);
    const c = MapFeedQuery(latitude: 37.5, longitude: 127.0, zoom: 17);

    expect(a, b);
    expect(a.hashCode, b.hashCode);
    expect(a == c, isFalse);
  });

  testWidgets('query가 null이면 아무것도 렌더하지 않는다', (tester) async {
    final query = ValueNotifier<MapFeedQuery?>(null);
    final controller = DraggableScrollableController();
    addTearDown(query.dispose);
    addTearDown(controller.dispose);

    await tester.pumpWidget(MaterialApp(
      home: Scaffold(
        body: Stack(
          children: [
            MapFeedSheet(
              query: query,
              controller: controller,
              onItemTap: (_) {},
            ),
          ],
        ),
      ),
    ));

    expect(find.byType(DraggableScrollableSheet), findsNothing);
  });
}
```

> `getMapFeed` 는 dotenv 초기화가 필요해 위젯 테스트에서 실데이터 경로를 태우지
> 않는다. 목 JSON 파싱은 Task 2 의 테스트가, 섹션 렌더는 Task 3 의 테스트가
> 이미 덮는다. 여기서는 **"보여줄 게 없으면 렌더하지 않는다"** 계약만 고정한다.

- [ ] **Step 3: 테스트 통과 확인**

Run: `flutter test test/screens/map/feed/map_feed_sheet_test.dart`
Expected: 2개 테스트 PASS

- [ ] **Step 4: 정적 분석**

Run: `flutter analyze`
Expected: `50 issues found.` (error 0)

---

### Task 5: MapBottomCard 확장 진입 + 주소 탭

**Files:**
- Modify: `lib/screens/map/bottom_card/map_bottom_card.dart` (필드 추가 / `initState` / `MapCardExpandedContent` 호출부)
- Modify: `lib/screens/map/bottom_card/map_card_expanded_content.dart` (필드 추가 / 주소 Row)

**Interfaces:**
- Consumes: 없음 (기존 위젯 확장)
- Produces:
  - `MapBottomCard` 에 3개 옵션 파라미터: `bool initialExpanded = false`, `BoardPost? initialDetail`, `VoidCallback? onLocationTap`
  - `MapCardExpandedContent` 에 1개 옵션 파라미터: `VoidCallback? onLocationTap`

**배경 (중요):** `MapBottomCard` 는 확장 시 `_loadDetail()` 로 **이미 `getBoardDetail` 을 호출한다**(`map_bottom_card.dart:406`). 그런데 `isLiked`/`fileInfoList` 는 그 detail 이 아니라 `MapPost` 에서 온다(`:830-836`, `_isCurrentLiked` → `_nav.currentPost.isLike`). 그래서 Task 6 이 상세를 미리 받아 `MapPost` 로 변환하고, 중복 호출을 막기 위해 그 `BoardPost` 를 `initialDetail` 로 씨딩한다. `_loadDetail` 의 `if (!force && _loadedDetail != null) return;` 가드가 그대로 동작한다.

- [ ] **Step 1: `MapBottomCard` 파라미터 추가**

`lib/screens/map/bottom_card/map_bottom_card.dart` — `final VoidCallback? onPostEdited;` 뒤에 추가:

```dart
  /// true 면 확장 상태로 바로 열린다 (피드 카드 탭 진입용).
  /// 기존 마커 탭 경로는 기본값 false 로 동작이 바뀌지 않는다.
  final bool initialExpanded;

  /// 미리 받아둔 상세. 주면 [initialExpanded] 진입 시 `getBoardDetail` 을
  /// 다시 호출하지 않는다 (피드 카드 탭은 이미 상세를 받아 MapPost 를 만든다).
  final BoardPost? initialDetail;

  /// 주소 탭 핸들러. null 이면 주소는 탭할 수 없다(기존 마커 탭 경로).
  final VoidCallback? onLocationTap;
```

생성자에 추가 (`this.onPostEdited,` 뒤):

```dart
    this.initialExpanded = false,
    this.initialDetail,
    this.onLocationTap,
```

- [ ] **Step 2: `initState` 에서 확장 상태 진입**

같은 파일 `initState` 의 `_pageController = PageController(...)` 뒤, 닫는 `}` 앞에 추가:

```dart
    if (widget.initialExpanded) {
      _cardState = _CardState.expanded;
      _loadedDetail = widget.initialDetail;
      // setState 를 initState 에서 부를 수 없으므로 다음 프레임에 처리한다.
      // initialDetail 이 있으면 _loadDetail 은 가드로 즉시 리턴한다.
      WidgetsBinding.instance.addPostFrameCallback((_) {
        if (!mounted) return;
        widget.onExpandedChanged?.call(true);
        unawaited(_loadDetail());
      });
    }
```

같은 파일 상단 import 에 `unawaited` 용으로 추가 (이미 있으면 생략):

```dart
import 'dart:async';
```

- [ ] **Step 3: `onLocationTap` 을 확장 콘텐츠로 전달**

같은 파일 `MapCardExpandedContent(` 호출부(`onEditTap: post.isOwner ? _navigateToEdit : null,` 뒤)에 추가:

```dart
            onLocationTap: widget.onLocationTap,
```

- [ ] **Step 4: `MapCardExpandedContent` 파라미터 추가**

`lib/screens/map/bottom_card/map_card_expanded_content.dart` — `final VoidCallback? onEditTap;` 뒤에 추가:

```dart
  /// 주소 탭 핸들러. null 이면 주소는 그냥 텍스트다.
  /// 피드 카드로 열린 경우에만 주어진다 — 탭하면 그 좌표로 카메라가 이동한다.
  final VoidCallback? onLocationTap;
```

생성자에 추가 (`this.onEditTap,` 뒤):

```dart
    this.onLocationTap,
```

- [ ] **Step 5: 주소 Row 를 탭 가능하게**

같은 파일의 주소 블록(현재 `if (post.streetName.isNotEmpty) ...[` ~ `],`, 대략 216-242행)을 아래로 **교체**한다:

```dart
                  if (post.streetName.isNotEmpty) ...[
                    const SizedBox(height: 2),
                    // onLocationTap 이 있으면 주소 전체가 탭 영역이 되고 우측에
                    // 이동 아이콘이 붙는다(탭 가능함을 알리는 신호).
                    GestureDetector(
                      onTap: widget.onLocationTap,
                      behavior: HitTestBehavior.opaque,
                      child: Row(
                        crossAxisAlignment: CrossAxisAlignment.start,
                        children: [
                          Padding(
                            padding: const EdgeInsets.only(top: 1),
                            child: Icon(Icons.location_on_outlined,
                                size: 13, color: colors.textTertiary),
                          ),
                          const SizedBox(width: 2),
                          Expanded(
                            child: Text(
                              post.streetName,
                              maxLines: 2,
                              overflow: TextOverflow.ellipsis,
                              style: TextStyle(
                                fontSize: 12,
                                fontFamily: 'Pretendard',
                                color: widget.onLocationTap != null
                                    ? colors.primaryStrong
                                    : colors.textTertiary,
                                height: 1.3,
                              ),
                            ),
                          ),
                          if (widget.onLocationTap != null)
                            Padding(
                              padding: const EdgeInsets.only(left: 2, top: 1),
                              child: Icon(Icons.my_location,
                                  size: 13, color: colors.primaryStrong),
                            ),
                        ],
                      ),
                    ),
                  ],
```

- [ ] **Step 6: 기존 마커 탭 회귀 확인**

Run: `flutter test test/screens/map/bottom_card/map_bottom_card_test.dart`
Expected: 기존 테스트 전부 PASS (새 파라미터는 모두 기본값이라 동작 변화 없음)

- [ ] **Step 7: 정적 분석**

Run: `flutter analyze`
Expected: `50 issues found.` (error 0)

---

### Task 6: map_naver 통합

**Files:**
- Modify: `lib/screens/map/map_naver.dart`

**Interfaces:**
- Consumes: Task 4 의 `MapFeedSheet`/`MapFeedQuery`/`kMapFeedPeekSize`, Task 5 의 `MapBottomCard` 새 파라미터, Task 1 의 `MapFeedItem`
- Produces: 없음 (최종 통합)

- [ ] **Step 1: import 추가**

`lib/screens/map/map_naver.dart` 상단 import 목록에 추가:

```dart
import 'package:unimal/screens/map/feed/map_feed_sheet.dart';
import 'package:unimal/service/map/models/map_feed.dart';
```

- [ ] **Step 2: 상태 필드 추가**

`_MapNaverState` 클래스 안, `final BubbleMarkerLayer _bubbleLayer = ...` 선언 근처에 추가:

```dart
  // ── 바텀카드 피드 (설계 docs/specs/2026-07-29-지도-바텀카드-피드.md) ──
  // 시트가 자기 데이터를 소유한다. 여기서는 조회 좌표만 알려주고 컨트롤러를
  // 보유한다(마커 탭 시 접어야 하므로).
  final ValueNotifier<MapFeedQuery?> _feedQuery =
      ValueNotifier<MapFeedQuery?>(null);
  final DraggableScrollableController _feedSheetController =
      DraggableScrollableController();

  /// 피드 카드로 열린 확장카드의 글. 마커 탭 경로(`_selectedGroupIndex`)와
  /// 별도다 — 피드 글은 `_postGroups` 에 없을 수 있다.
  MapPost? _feedSelectedPost;

  /// 위 글의 상세. `MapBottomCard` 가 `getBoardDetail` 을 다시 호출하지 않도록
  /// 씨딩한다.
  BoardPost? _feedSelectedDetail;

  bool _isOpeningFeedPost = false;
```

- [ ] **Step 3: `dispose` 에 정리 추가**

`dispose()` 안 `_bubbleLayer.cancelTimers();` 근처에 추가:

```dart
    _feedQuery.dispose();
    _feedSheetController.dispose();
```

- [ ] **Step 4: `_isAnyCardOpen` 에 피드 카드 포함**

기존 (약 212행):

```dart
  bool get _isAnyCardOpen => _selectedSymbol != null || _selectedPosts.isNotEmpty;
```

교체:

```dart
  // 피드 확장카드도 포함 — 열린 동안 검색바·하단 버튼·피드 시트가 함께
  // 페이드아웃되어 기존 마커 카드와 동일하게 동작한다.
  bool get _isAnyCardOpen =>
      _selectedSymbol != null ||
      _selectedPosts.isNotEmpty ||
      _feedSelectedPost != null;
```

- [ ] **Step 5: 마커 재조회 성공 시 피드 쿼리 갱신**

`_loadMapMarkers` 안 `outcome == _MapMarkerLoadOutcome.applied && mounted` 블록에서 `_lastSuccessfulMapRefreshAt = DateTime.now();` 바로 뒤에 추가:

```dart
        // 피드 시트에 현재 조회 지점을 알린다. 실제 조회 여부(시트가 펼쳐졌는지)는
        // 시트가 판단한다 (설계 §3).
        _feedQuery.value = MapFeedQuery(
          latitude: latitude,
          longitude: longitude,
          zoom: zoom,
        );
```

- [ ] **Step 6: 피드 카드 탭 핸들러 추가**

`_MapNaverState` 안, `_closeAllCards()` 메서드 앞에 추가:

```dart
  /// 피드 카드 탭 → 확장카드를 연다. **카메라는 움직이지 않는다.**
  ///
  /// 피드 응답은 서버에서 캐시되고 `isLike`/`isOwner`/전체 이미지 목록이 빠져
  /// 있으므로 상세를 받아야 확장카드를 제대로 채울 수 있다(설계 §5).
  Future<void> _openFeedPost(MapFeedItem item) async {
    if (_isOpeningFeedPost) return;
    _isOpeningFeedPost = true;
    _focusNode.unfocus();
    try {
      final detail = await BoardApiService().getBoardDetail(item.boardId);
      if (!mounted) return;
      setState(() {
        _feedSelectedDetail = detail;
        _feedSelectedPost = MapPost(
          id: detail.boardId,
          nickname: detail.nickname,
          profileImage: detail.profileImage,
          title: detail.title,
          content: detail.content,
          streetName: detail.streetName,
          // 상세에 좌표가 없으면 피드 아이템 좌표로 폴백 — 주소 탭 이동에 쓰인다.
          latitude: detail.latitude ?? item.latitude,
          longitude: detail.longitude ?? item.longitude,
          createdAt: detail.createdAt,
          fileInfoList: detail.fileInfoList,
          likeCount: detail.likeCount,
          replyCount: detail.replyCount,
          // score 는 마커 globalZIndex 전용이라 카드에서 쓰이지 않는다.
          score: 0,
          isOwner: detail.isOwner,
          isLike: detail.isLike,
        );
      });
    } catch (e) {
      debugPrint('[map] 피드 글 상세 조회 실패 ${item.boardId}: $e');
      // getBoardDetail 이 이미 사용자 알럿을 띄운다 — 여기선 카드를 열지 않는다.
    } finally {
      _isOpeningFeedPost = false;
    }
  }

  /// 피드 확장카드 닫기.
  ///
  /// 마커 탭 경로의 `_applySelectionHighlight(null)` /
  /// `_resumeAutomaticReloadsAfterInteractionClose()` 는 부르지 않는다 —
  /// 피드 카드는 마커를 선택한 적이 없고 자동 재조회를 멈춘 적도 없다.
  void _closeFeedPost() {
    if (_feedSelectedPost == null) return;
    setState(() {
      _feedSelectedPost = null;
      _feedSelectedDetail = null;
    });
  }

  /// 주소 탭 → 카드를 닫고 그 좌표로 카메라 이동.
  void _moveCameraToFeedPost() {
    final post = _feedSelectedPost;
    if (post == null) return;
    _closeFeedPost();
    _mapController?.updateCamera(
      NCameraUpdate.scrollAndZoomTo(
        target: NLatLng(post.latitude, post.longitude),
        zoom: kClusterExpandZoom,
      ),
    );
  }
```

- [ ] **Step 7: 마커 선택 시 피드 시트 접기**

`_selectMarker` 안 맨 앞(`final camera = await ...` 보다 위)에 추가:

```dart
    // 마커 탭과 피드 시트는 배타적 — 시트를 peek 으로 접는다.
    _collapseFeedSheet();
```

그리고 `_collapseFeedSheet` 를 `_openFeedPost` 앞에 추가:

```dart
  /// 피드 시트를 peek 으로 접는다. 컨트롤러가 아직 시트에 붙지 않았으면
  /// (섹션 0개로 시트가 렌더되지 않은 경우) 조용히 넘어간다.
  void _collapseFeedSheet() {
    if (!_feedSheetController.isAttached) return;
    unawaited(_feedSheetController.animateTo(
      kMapFeedPeekSize,
      duration: const Duration(milliseconds: 220),
      curve: Curves.easeOut,
    ));
  }
```

- [ ] **Step 8: Stack 에 피드 시트 배치**

`build` 의 Stack children 에서 **`// POI 심볼 탭 시 하단 장소 카드` 주석이 있는 `AnimatedPositioned` 바로 앞**에 삽입한다 (z-order 상 마커 카드보다 아래여야 한다):

```dart
          // 바텀카드 피드 — 상시 peek. 카드가 열리면 페이드아웃(배타적).
          // 검색바와 같은 IgnorePointer + AnimatedOpacity 패턴.
          IgnorePointer(
            ignoring: _isAnyCardOpen,
            child: AnimatedOpacity(
              duration: const Duration(milliseconds: 200),
              opacity: _isAnyCardOpen ? 0 : 1,
              child: MapFeedSheet(
                query: _feedQuery,
                controller: _feedSheetController,
                onItemTap: (item) => unawaited(_openFeedPost(item)),
              ),
            ),
          ),
```

- [ ] **Step 9: 피드 확장카드 렌더**

같은 Stack 에서 **마커 카드 `AnimatedPositioned` 블록 바로 뒤**에 삽입:

```dart
          // 피드 카드로 열린 확장카드 — 마커 카드와 별도 경로.
          if (_feedSelectedPost != null)
            Positioned(
              left: 0,
              right: 0,
              bottom: 0,
              child: MapBottomCard(
                key: ValueKey('feed-${_feedSelectedPost!.id}'),
                groups: [
                  [_feedSelectedPost!]
                ],
                initialGroupIndex: 0,
                initialExpanded: true,
                initialDetail: _feedSelectedDetail,
                minTopMargin: MediaQuery.sizeOf(context).height * 0.15 +
                    64 +
                    MediaQuery.paddingOf(context).bottom,
                onClose: _closeFeedPost,
                onLocationTap: _moveCameraToFeedPost,
              ),
            ),
```

- [ ] **Step 10: 정적 분석**

Run: `flutter analyze`
Expected: `50 issues found.` (error 0)

---

### Task 7: 전체 검증 + 커밋

**Files:** 없음 (검증만)

- [ ] **Step 1: 전체 테스트**

Run: `flutter test`
Expected: 전부 PASS. 신규 14개(Task 1: 7, Task 3: 5, Task 4: 2) 포함.

- [ ] **Step 2: 정적 분석 최종 확인**

Run: `flutter analyze`
Expected: `50 issues found.` — error 0, 기준선과 동일한 숫자

- [ ] **Step 3: 실기 확인 (iOS 시뮬레이터)**

Run: `flutter run`

확인 목록:
1. 지도 진입 → 하단에 얕은 시트(핸들 + "방금 올라온 소식" 헤더 + 카드 상단)가 보인다
2. 좌우 하단 버튼(내 위치 / 내 지도)이 시트에 가리지 않는다
3. 시트를 위로 끌면 3섹션(LATEST 5 / HOT 3 / NEARBY 4)이 보이고, `HOT` 헤더에만 `>` 가 없다
4. 썸네일 없는 카드(`mock-l3`)가 본문 스니펫 타일로 보인다
5. 제목 없는 카드(`mock-l2`)가 본문을 제목 자리에 보여준다
6. 피드 카드 탭 → **확장카드가 바로 열리고 카메라는 그대로**
   (목 `boardId` 는 서버에 없으므로 상세 조회가 실패하고 알럿이 뜬다 — 정상.
   이 단계 검증은 서버 구현 후 다시 한다)
7. 마커 탭 → 피드 시트가 페이드아웃되고 마커 카드가 올라온다. 카드를 닫으면 시트 복귀
8. 기존 마커 탭 흐름(스택 펼침, 좌우 스와이프, 확장, 댓글)이 그대로 동작한다

> 6번은 목 데이터의 한계다. 서버가 붙기 전까지 "탭 → 확장카드 + 주소 탭 이동" 은
> 검증할 수 없다. 시트·카드 렌더와 공존 동작(1~5, 7, 8)만 이 단계에서 확정한다.

- [ ] **Step 4: 커밋 (이번 작업의 유일한 커밋)**

```bash
git add lib/service/map/models/map_feed.dart \
        lib/service/map/map_feed_mock.dart \
        lib/service/board/board_api_service.dart \
        lib/screens/map/feed/ \
        lib/screens/map/bottom_card/map_bottom_card.dart \
        lib/screens/map/bottom_card/map_card_expanded_content.dart \
        lib/screens/map/map_naver.dart \
        test/service/map/models/map_feed_test.dart \
        test/screens/map/feed/ \
        .env.local \
        docs/specs/2026-07-29-지도-바텀카드-피드.md \
        docs/plans/2026-07-29-지도-바텀카드-피드-구현.md
```

커밋 메시지:

```
[feat] 메인 지도 바텀카드 피드 — 상시 peek 시트 + 섹션 카루셀

지도 하단에 상시 바텀시트를 두고 GET /board/map/feed 의 섹션 피드를 보여준다.
마커를 하나하나 누르지 않아도 "지금 이 지역에 뭐가 올라왔는지" 알 수 있다.

- 모델: MapFeedResponse/Section/Item. 이 API는 camelCase 다 (앱에서 snake_case 를
  쓰는 모델은 MapPost 뿐이라 그걸 참고하면 전부 null 이 된다). 모르는 섹션 타입은
  파싱 단계에서 그 섹션만 버려 서버가 앱 배포 없이 섹션을 늘릴 수 있게 한다.
- 상태 소유: 시트가 자기 데이터를 갖는다. map_naver 는 ValueNotifier<MapFeedQuery>
  갱신과 DraggableScrollableController 보유만 한다 (이미 2,900줄이라 상태를 더
  얹지 않는다). 재조회는 진입 1회 + 시트가 펼쳐졌을 때만 — peek 에서 계속 받으면
  안 보이는 내용을 위해 API 를 두 배로 쓴다.
- 카드 탭 → 확장카드. 카메라는 움직이지 않는다. 피드 응답은 서버에서 캐시되고
  isLike/isOwner/전체 이미지가 빠져 있어 getBoardDetail 로 상세를 받아 MapPost 로
  변환한다. 그 BoardPost 를 initialDetail 로 씨딩해 카드가 같은 상세를 두 번
  받지 않게 했다.
- 위치 이동은 주소 탭 — 카드를 닫고 그 좌표로 줌 17.5 이동.
- 마커 카드와 배타적. 검색바가 쓰는 IgnorePointer + AnimatedOpacity 패턴 재사용,
  마커 선택 시 시트를 peek 으로 접는다. isInteractionOpen 에는 넣지 않는다 —
  넣으면 시트가 열린 동안 지도 재조회가 멈춰 피드도 갱신되지 않는다.
- 섹션이 0개면(빈 결과·실패·서버 미구현) 시트를 아예 렌더하지 않는다.

서버 미구현: /board/map/feed 는 unimal-server 에 설계 문서만 있고 코드가 없다.
.env.local 의 MAP_FEED_MOCK=true 로 목 JSON 을 실제 파서에 태워 UI 를 확인한다
(릴리즈는 .env.prod 를 쓰고 키가 없어 자동 비활성). 서버가 붙으면 키와
map_feed_mock.dart, getMapFeed 의 분기 3줄을 지운다.

미검증: 목 boardId 는 서버에 없어 "카드 탭 → 확장카드"와 "주소 탭 → 이동"은
서버 구현 후에 확인해야 한다. 시트 렌더·공존 동작·기존 마커 흐름은 확인했다.

설계: docs/specs/2026-07-29-지도-바텀카드-피드.md
계획: docs/plans/2026-07-29-지도-바텀카드-피드-구현.md

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
```

---

## 자체 검토 결과

**스펙 커버리지** — 설계 문서 각 절 대응:

| 설계 절 | 태스크 |
| --- | --- |
| §2 데이터 계층 / camelCase / unknown 스킵 / 주소 파라미터 | Task 1, 2 |
| §3 상태 소유 / 재조회 시점 | Task 4(시트 판정), Task 6(notifier 갱신) |
| §4 UI 파일 구조 / peek / 광고 자리 | Task 3, 4 |
| §5 카드 탭 → 확장카드 | Task 5, 6 |
| §6 주소 탭 이동 | Task 5, 6 |
| §7 마커 카드 공존 | Task 6 (Step 4, 7, 8) |
| §8 빈 결과·실패 | Task 4 (`sections.isEmpty` → `SizedBox.shrink`) |
| §9 목 데이터 | Task 2 |
| §10 테스트 | Task 1, 3, 4 |

**광고 자리(§4)**: `MapFeedSheet.build` 의 `for (final section in sections) ...[]` 가 위젯 리스트를 조립하는 형태다. 광고를 끼우는 것이 이 리스트에 한 항목 추가하는 일이 된다 — 별도 코드 변경 없이 요구를 만족한다.

**타입 일관성 확인**:
- `MapFeedSectionType.tryParse` → Task 1 에서 정의, Task 1 내부에서만 사용 ✓
- `MapFeedSection.tryFromJson` (not `fromJson`) → Task 1 정의, Task 1 내부 사용 ✓
- `decodeMapFeedResponse` → Task 1 정의, Task 2 에서 사용 ✓
- `kMapFeedPeekSize` → Task 4 정의, Task 6 `_collapseFeedSheet` 에서 사용 ✓
- `kMapFeedCardWidth` / `kMapFeedRowHeight` → Task 3 정의·사용 ✓
- `MapFeedQuery` → Task 4 정의, Task 6 에서 생성 ✓
- `initialExpanded` / `initialDetail` / `onLocationTap` → Task 5 정의, Task 6 에서 사용 ✓
- `BoardPost.latitude`/`longitude` 는 `double?` 이라 Task 6 에서 `?? item.latitude` 폴백 ✓

**플레이스홀더 스캔**: "TBD"/"적절히"/"에러 처리 추가" 없음. 모든 코드 스텝에 실제 코드가 있다 ✓

## 알려진 한계

1. **서버 미구현** — 목 `boardId` 로는 상세 조회가 실패하므로 Task 7 Step 3 의 6번 항목을 검증할 수 없다.
2. **`isLike` 신선도** — 마커 탭 경로도 `MapPost.isLike`(30초 캐시 가능)를 쓴다. 확장 시 받은 `_loadedDetail` 을 좋아요 상태에 반영하지 않는 기존 동작은 그대로 둔다. 별도 작업.
3. **`MapFeedSheet` 실데이터 경로 위젯 테스트 없음** — `getMapFeed` 가 dotenv 초기화를 요구한다. 파싱은 Task 1·2, 렌더는 Task 3 이 덮는다.

---

# ⚠️ 실행 결과 — 미완료 상태로 커밋 (2026-07-29)

Task 1~6 구현 완료, **Task 7(실기 검증) 미실행.** 다른 환경에서 검증을 이어가기 위해
이 상태로 커밋했다.

## 검증된 것 / 안 된 것

| | 상태 |
| --- | --- |
| `flutter analyze` | ✅ 50 issues (error 0, 기준선 동일) |
| `flutter test` | ✅ **102/102 통과** |
| Task 1~5 코드 리뷰 | ✅ 전부 clean (Task 3·4 는 fix 1라운드 후) |
| Task 6 코드 리뷰 | ❌ **Important 3건 미수정** (아래) |
| 실기 동작 | ❌ **전혀 확인 안 됨** |

## 🔴 다른 환경에서 시작하기 전에 — 목 플래그를 추가해야 한다

`.env.local` 은 gitignore 되므로 **이 커밋에 포함되지 않는다.** 목 데이터로 UI 를 보려면
그 파일 끝에 한 줄을 직접 추가하라:

```
MAP_FEED_MOCK=true
```

없으면 `getMapFeed` 가 실제 API(`/board/map/feed`, **서버 미구현**)를 호출해 null 을
반환하고, 설계상 시트가 렌더되지 않는다 — 즉 **아무것도 안 보이는 것이 정상 동작**이다.

## 🔴 미해결 Important 3건 (Task 6 리뷰 발견, 수정 안 됨)

### #1 피드 카드를 닫으면 지도 자동 재조회가 정지한다

`map_naver.dart` `_closeFeedPost`. **실제 버그다.**

`_isAnyCardOpen` 에 `_feedSelectedPost` 를 넣었으므로(§7) 피드 카드가 열린 동안
`_canAutoRefresh` 가 false 가 된다. 그 사이 30초 신선도 타이머가 발화하면
`_onFreshnessDue` 가 `_deferFreshness` 후 **타이머를 다시 걸지 않고 return** 한다.
그 뒤 카드를 닫아도 `_closeFeedPost` 는 `setState` 만 하므로 **pending 을 소비할 주체가
없다** → 사용자가 가만히 있으면 지도가 다시 갱신되지 않는다.

마커 카드·검색 해제·`_closeAllCards` 는 모두
`_resumeAutomaticReloadsAfterInteractionClose()` 를 부르며, 그 불변식은 계약 테스트
(`map_reload_integration_contract_test.dart` 의 "검색 interaction 해제는 pending과
spatial 재평가를 한 번 재개한다", "카드 close는 idle 재평가로 말풍선을 복원하고 pending을
소비한다")로 못박혀 있다. **피드 카드가 그 불변식을 깨는 유일한 interaction blocker 다.**

**이 설계 문서 §5 의 근거가 사실과 다르다**: "자동 재조회를 멈춘 적도 없다" 고 적었지만
§7 의 `_isAnyCardOpen` 결정 때문에 실제로는 멈춘다. 두 절이 모순이다.

수정: `_closeFeedPost` 끝에 `unawaited(_consumePendingFreshness());` 추가.
(말풍선 복원은 마커를 건드린 적 없어 `_onCameraIdle` 까지 부를 필요는 없다)

> 주소 탭 경로는 우연히 무사하다 — 카메라 이동이 `onCameraIdle` 을 유발해 pending 이
> 소비된다. 정지는 **닫기 경로 한정**이다.

### #2 상시 peek 시트가 '내 위치'·'내 지도' 버튼을 덮는다 — 설계 전제 오류

**이 문서 §4 의 "좌우 하단 버튼도 그대로 살아 있어 재배치가 없다" 는 틀렸다.**

두 버튼은 `bottom: 45`, 크기 36 → 하단에서 45~81pt. 시트 peek 은
`kMapFeedPeekSize`(0.15) × Stack 높이이고, 이 화면은 `root_screen` 의 `IndexedStack`
body 라 Stack 높이 ≈ 화면 − (네비바 64 + 하단 safe area):

- iPhone 14: ≈ 746 → peek ≈ **112pt**
- iPhone SE: ≈ 603 → peek ≈ **90pt**

**어느 기기에서든 81pt 를 넘어 두 버튼이 완전히 가려진다.** 시트는 `colors.surface`
불투명이고 내부 `ListView` 가 포인터를 받으므로 시각적으로도, 탭으로도 죽는다.
내 위치 버튼은 지도 기본 컨트롤이라 기존 동작 훼손이다.

선택지(**UX 결정이 필요하다**):
1. 두 버튼의 `bottom` 을 peek 높이 + 여유로 올린다 (`height * kMapFeedPeekSize + 12`)
2. 시트를 Stack 에서 두 버튼 **앞에** 삽입해 버튼이 시트 위에 뜨게 한다
3. peek 을 버튼 높이 아래로 줄인다 (0.15 → 약 0.10, 카드 상단이 덜 보임)

### #3 피드 확장카드에 `onPostEdited` 를 넘기지 않았다

`map_naver.dart` 의 `MapBottomCard(key: ValueKey('feed-...'))` 블록.

본인 글이면 그 카드에서 수정/삭제가 가능하고 `MapBottomCard` 가 `onPostEdited` 를
부르는데, 피드 경로는 이 콜백을 주지 않는다. → **글을 삭제해도 카드가 그대로 열려
삭제된 글을 보여주고, 지도 마커도 갱신되지 않는다.** 그 상태에서 좋아요/댓글을 누르면
서버 오류 알럿이 난다. (마커 카드 경로는 `_closeAllCards()` + `refreshMap()` 을 한다)

수정: `onPostEdited: () { _closeFeedPost(); refreshMap(); }`

## 🟡 미해결 Minor

- **주소 탭 후 펼쳐진 시트(0.9)가 방금 이동한 지도를 덮는다.** `_moveCameraToFeedPost` 가
  `_collapseFeedSheet()` 를 부르지 않는다. 마커 선택 경로와 대칭이 되도록 한 줄 추가 권장.
- **상세 조회 중 시각 피드백이 없다.** `_isOpeningFeedPost` 는 `setState` 밖 순수 가드라
  카드 탭 후 네트워크 왕복 동안 무반응이고, 그 사이 다른 카드 탭은 조용히 무시된다.
- `kMapFeedRowHeight` 178 vs 실제 자식 ~169-171 → 7-9px 여유 (overflow 없음).
- `map_card_expanded_content.dart` 에 `Colors.white` 2개 잔존 (전송 버튼 스피너·아이콘).
  `CLAUDE.md` 보이스카웃 규칙은 파일 단위라 기술적으로 범위 안이지만, 컬러 버튼 위 흰
  아이콘이라 매핑할 토큰이 없다 — `onPrimary` 류 시멘틱 토큰 추가가 선행돼야 한다.
- 기본(peek) 카드의 주소는 `onLocationTap` 배선이 없어 탭 불가 (피드는 확장 상태로 열리므로
  실사용 영향은 낮음).

## 실기 확인 목록 (Task 7, 미실행)

1. 지도 진입 → 하단에 얕은 시트(핸들 + "방금 올라온 소식" 헤더 + 카드 상단)가 보인다
2. **하단 버튼 2개가 가려지는지 확인** → 가려지면 위 #2
3. 시트를 올리면 3섹션(LATEST 5 / HOT 3 / NEARBY 4), `HOT` 헤더에만 `>` 없음
4. 썸네일 없는 카드(`mock-l3`)가 본문 스니펫 타일로 보인다
5. 제목 없는 카드(`mock-l2`)가 본문을 제목 자리에 보여준다
6. 피드 카드 탭 → 확장카드가 열리고 카메라는 그대로
   (목 `boardId` 는 서버에 없어 **상세 조회가 실패하고 알럿이 뜬다 — 정상.** 이 항목은
   서버 구현 후에 다시 확인해야 한다)
7. 마커 탭 → 피드 시트 페이드아웃 + 마커 카드 상승, 닫으면 시트 복귀
8. **기존 마커 흐름 회귀 확인**: 스택 펼침, 좌우 스와이프, 확장, 댓글, 좋아요, 수정
9. **#1 재현 확인**: 피드 카드를 30초 이상 열어둔 뒤 닫고, 지도를 가만히 둔 채 새 글이
   반영되는지 본다 (안 되면 #1 확정)

---

# 최종 리뷰 완료 (2026-07-30)

Task 1~6 태스크 리뷰 통과 후 **전체 브랜치 리뷰**를 돌렸다. 태스크별 리뷰가 못 본
**태스크 경계 문제 2건**과 앱 측 방어 1건이 나왔고, 전부 수정했다.

## 위 "실행 결과" 절의 미해결 3건 → 전부 수정됨

| | 상태 |
| --- | --- |
| #1 피드 카드 닫기 후 재조회 정지 | ✅ `_consumePendingFreshness()` 추가 |
| #2 peek 시트가 하단 버튼 가림 | ✅ 버튼을 `bottomButtonOffset` 으로 올림 + **시트 부재 시 45 로 복귀** |
| #3 `onPostEdited` 미전달 | ✅ 추가 |

## 최종 리뷰가 추가로 잡은 것 (수정 완료)

### 버튼 오프셋이 시트 부재 시에도 적용됐다

`#2` 를 고칠 때 오프셋을 **무조건** 적용했다. 그런데 서버에 `/board/map/feed` 가 없으면
시트가 영구 미렌더(`SizedBox.shrink`)인데 버튼만 ~126pt 떠서 **허공에 놓인다.**
병합하면 서버 작업이 끝나기 전까지 모든 사용자가 그 상태를 본다.

→ `MapFeedSheet` 에 `onContentChanged` 를 추가해 내용 유무가 **바뀔 때만** 통지하고,
`map_naver` 가 `_hasFeedContent` 로 받아 오프셋을 조건부로 계산한다(없으면 45).

### 피드 카드가 열린 채 마커를 탭하면 카드 2개가 동시에 마운트됐다

피드 카드 `minTopMargin` ≈ 224pt 라 **화면 상단 지도가 여전히 탭 가능**한데 `NaverMap` 에는
`_isAnyCardOpen` 가드가 없다. `_feedSelectedPost` 를 지우는 곳이 `_closeFeedPost` 뿐이어서
(a) 마커 탭 시 두 카드가 겹치고 (b) **지도 탭으로 피드 카드가 닫히지 않았다** — 사용자가
배운 "지도 탭 = 닫기"가 깨진다.

→ `_closeAllCards` / `_selectMarker` 의 `setState` 에서 피드 카드 상태를 직접 null 로
둔다(`_closeFeedPost()` 를 부르면 불필요한 재조회가 돈다). 재발 방지로 이 레포의
소스-텍스트 계약 테스트 관용구를 써서 계약을 못박았다.

### 파서의 조용한 기본값이 계약 위반을 숨겼다

서버가 실수로 camelCase 로 응답하면 `board_id` → `''`, `like_count` → 0,
`created_at` → `''`(→"방금 전") 인데 `type`/`title`/`content` 는 한 단어라 그대로
매칭된다. 결과: **섹션이 정상 렌더되고 "좋아요 0 · 방금 전" 쓰레기 카드가 뜬다.**

→ `boardId` 빈 아이템을 버려 계약 위반이 "피드가 안 뜬다"로 즉시 드러나게 했다.
리뷰어가 서버 `MapPostInfo.kt` 를 직접 읽어 **snake_case 기대가 맞는 방향임을 확인**했다.

## 검증

```
flutter analyze  → 50 issues (error 0, 기준선)
flutter test     → 104/104 (기존 102 + 신규 2)
```

## 남은 일 — 실기 검증만

**여전히 실기 동작을 한 번도 확인하지 않았다.** 위 "실기 확인 목록" 9개를 그대로 쓰면
된다. 목 플래그(`MAP_FEED_MOCK=true`)가 `.env.local` 에 필요하다.

## 후속 작업 (병합 차단 아님, 우선순위 순)

1. **서버 스펙 문서 정정** — `unimal-server` 의
   `docs/specs/2026-07-29-지도-바텀카드-피드-api.md` 가 아직 camelCase 다. 서버 구현
   착수 전에 snake_case + 클래스 레벨 `@JsonNaming(SnakeCaseStrategy)` 로 고쳐야 한다.
2. **`kMapFeedRowHeight` 접근성** — 178 vs 자식 ~169-171 의 여유가 아니라 **시스템 글꼴
   확대(×1.3)에서 184 > 178 로 overflow** 가 난다. 큰 글꼴 사용자 전원에게 재현되므로
   `MediaQuery.textScalerOf` 기반 산출이나 제목 `Flexible` 처리가 필요하다.
3. **`onPrimary` 시멘틱 토큰 도입** — `map_card_expanded_content.dart` 의 `Colors.white`
   2개(전송 버튼 스피너·아이콘)는 컬러 버튼 위 흰 아이콘이라 매핑할 토큰이 없다.
   `app_colors.dart` 에 light/dark 둘 다 추가하고 전 화면을 함께 치환하는 별도 작업.
4. **에러 vs 빈 결과 구분** — 지금은 둘 다 "아무것도 없음" 이다. 서버가 붙은 뒤
   "응답은 왔지만 sections 0개 → 조용히 / 네트워크·파싱 실패 → 재시도" 로 나눈다.
5. **peek 카드 주소 탭** — 확장 상태에서만 탭 가능해서, 카드를 축소하면 파란 주소가
   회색으로 죽는다. "방금 탭 가능했던 것이 사라짐" 이라 단순 미구현보다 혼란스럽다.
6. **콜드 스타트 경합** — 서버가 붙으면 첫 마커 로드 직후 피드 조회 + 카드 이미지 3~4장이
   마커 썸네일 디코드와 겹친다. 이 프로젝트는 콜드 스타트를 따로 최적화한 이력이 있다
   (`5a6a757`). 첫 조회를 두 번째 마커 리로드로 미루는 편이 안전하다.
7. **목 활성 조건을 `!kReleaseMode` 로 강화** — 실질 위험은 0이지만(`.env.local` 이
   `localhost:8080` 이라 ENV=local 릴리즈는 스모크 테스트를 통과할 수 없고, `.gitignore`
   가 `.env*` 를 배제해 목 키가 레포에 없다) 한 토큰으로 구조적 차단이 가능하다.
   **주의: 다른 개발자가 클론하면 목 키가 없어 피드가 아예 안 보인다.**
8. 조회 중 도착한 쿼리 유실, `_isLoading` 이 `build` 에서 안 읽히는데 `setState` 유발,
   POI 탭이 시트를 안 접는 비대칭.
