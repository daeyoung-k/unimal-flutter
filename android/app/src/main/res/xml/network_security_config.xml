<?xml version="1.0" encoding="utf-8"?>
<!--
  로컬 개발용 cleartext(HTTP) 허용 — Android 9+(API 28)는 기본적으로
  cleartext 트래픽을 차단해, .env.local 의 http://10.0.2.2:8080 (에뮬레이터의
  호스트 루프백) API 호출이 전부 실패한다 (2026-07-15 마이페이지 무한 스피너).
  허용 대상을 개발용 루프백 도메인으로만 한정 — 실서버(.env.prod)는 https 라
  이 설정의 영향을 받지 않고, 그 외 도메인의 cleartext 는 계속 차단된다.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
